/*
解题思路：
如果说“打家劫舍”是一维 DP 的巅峰，“不同路径”是二维网格 DP 的基石，那么这道“最长公共子序列（LCS）”绝对是所有字符串 DP 问题的祖师爷！

这道题的解题模板极其经典，只要你把它吃透，以后遇到什么“编辑距离”、“两个字符串的删除操作”，统统都是这套代码稍微改两行就能秒杀！

1. 核心思维：两两比对，神仙打架
既然有两个字符串，我们自然而然地需要一个二维数组来记录它们之间的匹配状态。

我们定义 dp[i][j] 表示：text1 的前 i 个字符，和 text2 的前 j 个字符，它们的最长公共子序列的长度。

灵魂拷问（状态转移方程）：
当我们正在考察 text1 的第 i 个字符（下标 i−1）和 text2 的第 j 个字符（下标 j−1）时，只有两种情况：

“喜相逢”（两个字符一模一样）：
比如 text1 是 "ab**c**"，text2 是 "a**c**"。最后都是 'c'！
既然这俩字符对上了，那它们俩就能手牵手加入“公共子序列”的队伍。
队伍的长度，就等于它俩出现之前的历史最长记录 + 1。
也就是：

dp[i][j]=dp[i−1][j−1]+1
“话不投机”（两个字符不一样）：
比如 text1 是 "ab**c**"，text2 是 "ac**e**"。'c' 和 'e' 不一样。
那怎么办？我们只能尝试“委曲求全”：

要不试试抛弃 text1 的 'c'，看看 "ab" 和 "ace" 能匹配出多长？（对应 dp[i−1][j]）

要不试试抛弃 text2 的 'e'，看看 "abc" 和 "ac" 能匹配出多长？（对应 dp[i][j−1]）
作为一个理性的算法，我们当然是把这两种退一步的情况都看一眼，然后挑个大的！
也就是：

dp[i][j]=max(dp[i−1][j],dp[i][j−1])
2. 🚨 关键技巧：多开一行一列（处理边界）
如果直接让 i 和 j 代表字符串的下标（从 0 开始），当你在算 dp[0][0] 的时候，公式里会出现 dp[−1][−1]，直接越界崩溃！

破局技巧：加入“空字符串”的虚拟状态！
我们把 DP 数组的行数和列数都多开一格（大小为 (m+1)×(n+1)）。

第 0 行：代表 text1 是个空字符串。空字符串和任何字符串的公共子序列都是 0。

第 0 列：代表 text2 是个空字符串。同理，结果全是 0。

这样一来，我们在处理真正的第 1 个字符时，它前面就有了一层安全可靠的“0 垫背”，再也不怕越界了！

3. C++ 满分标准模板代码
只要理解了上面的逻辑，代码写出来极其工整、赏心悦目：

C++
#include <vector>
#include <string>
#include <algorithm>

using namespace std;

class Solution {
public:
    int longestCommonSubsequence(string text1, string text2) {
        int m = text1.length();
        int n = text2.length();

        // 创建一个 (m+1) x (n+1) 的二维数组，并全部初始化为 0
        // dp[i][j] 代表 text1 前 i 个字符和 text2 前 j 个字符的 LCS 长度
        vector<vector<int>> dp(m + 1, vector<int>(n + 1, 0));

        // i 和 j 从 1 开始遍历，对应字符串里的下标就是 i-1 和 j-1
        for (int i = 1; i <= m; i++) {
            for (int j = 1; j <= n; j++) {
                // 情况 1：字符相等，天作之合
                if (text1[i - 1] == text2[j - 1]) {
                    // 等于左上角的值 + 1
                    dp[i][j] = dp[i - 1][j - 1] + 1;
                } 
                // 情况 2：字符不等，委曲求全
                else {
                    // 等于 左边 和 上边 中的最大值
                    dp[i][j] = max(dp[i - 1][j], dp[i][j - 1]);
                }
            }
        }

        // 最终答案就在表格的最右下角
        return dp[m][n];
    }
};
4. 复杂度分析
时间复杂度：O(M×N)。两个 for 循环遍历了整个二维表格，每次操作只需要常数时间。

空间复杂度：O(M×N)。开辟了一个二维数组。

(注：如果你还记得我们在“不同路径”里学过的滚动数组神技，这里的空间复杂度其实也可以压缩到 O(min(M,N))，只需要两行数组滚动覆盖即可。但在面试中，对于字符串匹配问题，能写出干净利落的二维数组解法已经完全足够了。)

顿悟时刻
这道题的转移方程深刻揭示了字符串处理的核心规律：相等则齐头并进（找左上角），不等则兵分两路（找左边和上边）。

*/

//我的代码：
class Solution {
public:
    int longestCommonSubsequence(string text1, string text2) {
        int m = text1.size();
        int n = text2.size();

        vector<vector<int>> dp(m+1,vector<int>(n+1,0));

        for (int i = 1; i <= m; i++)
        {
            for (int j = 1; j <= n; j++)
            {
                if (text1[i-1] == text2[j-1])
                {
                    dp[i][j] = dp[i - 1][j - 1] + 1;
                }
                else
                {
                    dp[i][j] = max(dp[i - 1][j], dp[i][j - 1]);
                }
            }
        }

        return dp[m][n];
    }
};
