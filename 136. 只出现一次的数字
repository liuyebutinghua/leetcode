/*
这道题（LeetCode 136. 只出现一次的数字）是位运算的绝对入门神题。就像爬楼梯是 DP 的 Hello World 一样，这道题就是位运算的 Hello World。

面对这道题，我们先来看看常规人的思路为什么会被题目的“苛刻条件”直接毙掉：

常规思路 1：哈希表（Hash Map / Set）。拿个本子记录，遇到没见过的数字记下来，见过的划掉。最后本子上剩下的就是那个单身狗。

死因：需要额外的本子，空间复杂度是 O(N)。题目严格要求只使用常量额外空间 O(1)，也就是不能用数组和哈希表。

常规思路 2：排序（Sort）。先把数组从小到大排好队，然后两两比较，哪两个没凑成对，单出来的就是它。

死因：最快的排序算法时间复杂度也是 O(N log N)。题目严格要求线性时间复杂度 O(N)，也就是只能大致遍历一遍数组，绝对不能排序。

既要马儿跑（遍历一次），又要马儿不吃草（不给额外内存）。这怎么搞？
这时候，就必须请出计算机底层最古老、最神奇的魔法了：异或运算（XOR，符号是 ^）。

1. 核心思维：异或运算的“正反物质湮灭”
在位运算的世界里，异或（^）有三条极其霸道且美妙的铁律：

自己跟自己异或，结果是 0：a ^ a = 0。
(这就好比正物质遇到了反物质，只要长得一模一样，砰的一声，同归于尽，直接湮灭消失！)

任何数字跟 0 异或，结果还是它自己：a ^ 0 = a。
(0 就是个透明人，谁跟它异或都不受影响。)

异或运算满足交换律和结合律：a ^ b ^ a  =  a ^ a ^ b  =  0 ^ b  =  b。
(也就是说，不论数字的先后顺序怎么打乱，只要是成对出现的，最后都会自动找到彼此并湮灭！)

2. 破局推演：nums = [4, 1, 2, 1, 2]
理解了上面的三条铁律，这道题的解法就变成了纯粹的“魔法”。

题目说：除了一个数字以外，其余全部出现了两次。
那我们就把数组里所有的数字全部连起来异或一遍！

按照交换律，无论原本顺序多乱，它在底层实际上等价于：
(1 ^ 1) ^ (2 ^ 2) ^ 4

1 ^ 1 湮灭了，变成 0。

2 ^ 2 湮灭了，变成 0。

现在剩下了：0 ^ 0 ^ 4。

根据铁律，0 跟任何数字异或都是那个数字本身。

最终结果：4！ 那个唯一落单的数字，像水落石出一样自己浮现出来了！

3. C++ 满分极客代码
这绝对是你刷 LeetCode 至今，写过的最短、最爽的代码，甚至都不需要什么大括号嵌套：

C++
#include <vector>

using namespace std;

class Solution {
public:
    int singleNumber(vector<int>& nums) {
        int result = 0; // 0 和任何数异或都是那个数本身，所以用 0 打底

        // 把数组里的所有数字全部异或一遍
        for (int num : nums) {
            result = result ^ num; 
            // 在 C++ 里，也可以简写为 result ^= num;
        }

        // 所有成对的数字都同归于尽了，剩下的就是那个单身狗
        return result;
    }
};
4. 复杂度分析
时间复杂度：O(N)。只把数组从头到尾遍历了一次，每次只做一次极快的底层 CPU 位运算。完美符合要求。

空间复杂度：O(1)。只用了一个 result 变量，没有开辟任何额外空间。完美符合要求。

顿悟时刻
你看，在 DP 里我们需要绞尽脑汁地寻找状态转移方程，而在位运算里，一旦你掌握了底层的数学规律，那些看似无解的苛刻条件瞬间就成了纸老虎。

*/

//我的代码：
class Solution {
public:
    int singleNumber(vector<int>& nums) {
        int result= nums[0];

        for (int num : nums)
        {
            result = result ^ num;
        }

        return result;
    }
};
