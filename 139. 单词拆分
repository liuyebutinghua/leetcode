/*
解题思路：
在零钱兑换里：你用硬币去拼凑总金额。
在单词拆分里：你用字典里的单词去拼凑长字符串。

背后的灵魂逻辑一模一样：都是在问“前面那一截能不能凑出来？如果能，加上最后这一块，是不是整体也能凑出来？”

1. 核心思维：寻找“完美切割点”
假设我们的字符串是 s="leetcode"，字典是 ["leet", "code"]。

我们怎么判断 "leetcode" 行不行？
如果你拥有上帝视角，你会说：“只要我知道前面的 'leet' 是合法的，并且剩下的 'code' 在字典里，那整个 'leetcode' 就是合法的！”

这就是动态规划的精髓。我们定义一个数组 dp，dp[i] 表示：字符串 s 的前 i 个字符能不能被完美拆分。

状态转移方程（灵魂发问）：
想要证明 dp[i] 是 true（前 i 个字符合法），我们需要在前面找一个切割点 j（j<i）。
只要同时满足以下两个条件，dp[i] 就是 true：

前面的基础得牢靠：dp[j] 必须是 true（也就是前 j 个字符已经被成功拼出来了）。

后面这截得在字典里：从 j 到 i 截取出来的这段子串，必须在字典 wordDict 里。

2. 手工推演：s = "leetcode"
准备一个大小为 9（字符串长度 8 + 1）的 dp 数组，全部初始化为 false。

终极边界：dp[0] = true。这代表“空字符串”是合法的（这和凑 0 元钱需要 0 个硬币是一个道理，它是所有拼接的基石）。

开始填表：

查 dp[1] ("l")：前面没有合法的切割点，dp[1] = false。

查 dp[2] ("le")：不行，dp[2] = false。

...

查 dp[4] ("leet")：

当切割点 j=0 时，dp[0] 是 true（地基稳固）！

并且，截取出来的子串 "leet" 在字典里！

完美！dp[4] = true。

...

查 dp[8] ("leetcode")：

切割点 j 一路往后试... 试到 j=4 时，发现 dp[4] 是 true（前面拼出过 "leet"）！

并且，截取出来的剩下的子串 "code" 也在字典里！

完美通关！dp[8] = true。

*/
//ai的代码：
class Solution {
public:
    bool wordBreak(string s, vector<string>& wordDict) {
        // 关键优化：把数组转成哈希集合，实现 O(1) 的超快单词查找
        unordered_set<string> wordSet(wordDict.begin(), wordDict.end());
        
        int n = s.length();
        // dp[i] 表示 s 的前 i 个字符能否被完美拆分
        vector<bool> dp(n + 1, false);
        
        // 边界条件：空字符串是所有有效拆分的基石
        dp[0] = true;

        // 外层循环 i：逐步考察前 1 个字符、前 2 个字符... 直到前 n 个字符
        for (int i = 1; i <= n; i++) {
            // 内层循环 j：寻找一个切割点 j
            // 把前 i 个字符劈成两半：前 j 个字符，和剩下的子串
            for (int j = 0; j < i; j++) {
                // 条件1：前 j 个字符必须合法 (dp[j] == true)
                // 条件2：剩下的从 j 开始、长度为 i-j 的子串，必须在字典里
                if (dp[j] && wordSet.count(s.substr(j, i - j))) {
                    dp[i] = true;
                    break; // 只要找到一种合法的切法，前 i 个字符就稳了，不用再往后试其他切法了！
                }
            }
        }

        // 最终答案就是整个字符串（前 n 个字符）能否被拆分
        return dp[n];
    }
};

//我的代码：
class Solution {
public:
    bool wordBreak(string s, vector<string>& wordDict) {
        unordered_set<string> wordSet(wordDict.begin(), wordDict.end());

        int n = s.size();
        vector<bool> dp(n + 1, false);
        dp[0] = true;

        for (int i=1;i<=n;i++)
        {
            for (int j = 0; j < i; j++)
            {
                if (dp[j] && wordSet.count(s.substr(j, i - j)))
                {
                    dp[i] = true;
                    break;
                }
            }

        }

        return dp[n];
    }
};



