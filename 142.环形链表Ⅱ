/*
这道题是面试场上的“绝杀题”。面试官让你写 141 题（判断有没有环），往往只是走个过场；他真正想要的，是让你在白板上把这道 142 题的数学公式推导出来！

一旦你把下面这个堪称“魔法”的推导过程甩在面试官脸上，他绝对会在心里直接给你打上“Strong Hire（强烈推荐录用）”的标签。

1. 核心思维：见证奇迹的数学推导
我们依然派出乌龟（走 1 步）和兔子（走 2 步）。假设他们在环内的某一点相遇了。
现在，我们来画一张图，并定义三个极简的变量：

L (直线距离)：从链表头部（head）到环的入口点的距离。

x (相遇点偏移量)：从环的入口点，到乌龟和兔子相遇点的距离。

C (环的周长)：环走一圈的总长度。

🌟 灵魂推导开始：
当乌龟和兔子相遇时：

乌龟走过的路程：它走过了直线 L，并在环里走了 x 才被追上。所以乌龟路程 = L+x。

兔子走过的路程：兔子跑得快，它不仅走过了直线 L，还在环里疯狂转了 n 圈，最后停在偏移量为 x 的位置。所以兔子路程 = L+nC+x。

根据物理学绝对法则：兔子的速度是乌龟的 2 倍，且他们跑的时间一样。
所以：兔子的路程 = 2 × 乌龟的路程

L+nC+x=2(L+x)
我们把括号拆开，化简一下这个方程式：

L+nC+x=2L+2x
L=nC−x
见证奇迹的时刻到了！
我们把 nC−x 稍微变个形，变成：

L=(n−1)C+(C−x)
这句话翻译成大白话是什么意思？

L 是从起点到入口的距离。

C−x 是从相遇点继续往前走，走到入口的距离。

(n−1)C 只是代表在环里白白转了几个整圈。

终极结论：
从“链表起点”走到“入口”的距离，竟然绝对等于从“相遇点”走到“入口”的距离！

破局战术：
当乌龟和兔子相遇后，我们把其中一个人（比如乌龟）拎回链表的起点 head。
然后，让乌龟和兔子以相同的速度（每次都走 1 步）同时往前走。
因为距离相等，他们下一次相遇的地方，绝对、一定、百分之百就是环的入口节点！

2. C++ 满分极客代码
带着这个神级结论，结合你刚刚在 141 题学到的“坠崖式刹车”，代码分两步走，清晰得像一篇散文：

C++
/**
 * Definition for singly-linked list.
 * struct ListNode {
 * int val;
 * ListNode *next;
 * ListNode(int x) : val(x), next(NULL) {}
 * };
 */


//我的代码（找环形的代码参考第141题）：
class Solution {
public:
    ListNode* detectCycle(ListNode* head) {
        if (head == nullptr || head->next == nullptr) {
            return nullptr;
        }
        ListNode* slow = head;
        ListNode* fast = head;
        bool ishuan = false;

        while (fast != nullptr && fast->next != nullptr)//找环
        {
            fast=fast->next->next;
            slow = slow->next;

            if (fast == slow)
            {
                ishuan = true;
                break;
            }
        }

        if (ishuan)//找到环
        {
            ListNode* new_head = head;
            while (new_head != slow)
            {
                new_head = new_head->next;
                slow = slow->next;
            }
            return new_head;

        }
         return nullptr;
  
    }
};
