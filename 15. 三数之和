/*
很多人一上来想用哈希表（就像做“两数之和”那样），结果被题目里那句“不可以包含重复的三元组”折磨得死去活来，光是写去重的逻辑就能写出上百行废代码。

这道题的唯一真神解法就是：排序 + 双指针降维打击！

1. 核心思维：降维打击，化三为二
要在毫无规律的数组里找三个数凑成 0，简直是大海捞针（暴力法需要 O(N^3) 的时间，绝对超时）。
怎么降维？
我们先“定住”一个数，剩下的问题不就变成了经典的“两数之和”了吗！

具体战术如下：

必须先排序！ 这是极其关键的一步。排序不仅能让双指针有方向感（大了就往左缩，小了就往右扩），更是为了后面能极其优雅地“去重”。

我们用一个 for 循环遍历数组，每次选定一个数 nums[i] 作为“老大”。

既然老大定下来了，我们需要找的另外两个小弟的和，就必须是 -nums[i]。

我们立刻派出两个指针：left 站在 i + 1 的位置，right 站在数组最末尾。

相向而行碰运气：

如果 nums[left] + nums[right] < 目标值：说明总和太小了，left 必须往右走一点（变大）。

如果 nums[left] + nums[right] > 目标值：说明总和太大了，right 必须往左走一点（变小）。

如果正好等于目标值：恭喜！记录下这个三元组！

2. 🚨 致命陷阱：如何优雅地“去重”？
这道题 90% 的人会死在“去重”上。因为数组里可能有多个一样的数字（比如 [-1, -1, 0, 1, 1]），如果你不加判断，就会挑出好几个 [-1, 0, 1]。

既然数组已经排好序了，相同的数字一定会紧紧挨在一起！
所以去重法则只有极其简单的两条：

老大的去重：如果当前的 nums[i] 和上一个考查过的 nums[i-1] 一模一样，直接跳过！因为上一个老大已经把能凑出来的组合全都找过一遍了。

小弟的去重：当 left 和 right 成功找到一组答案后，如果 left 的下一个数字和它一样，left 继续往右跳；如果 right 的前一个数字和它一样，right 继续往左跳。

3. C++ 满分工业级代码
加上了一个极其神仙的“早退优化（剪枝）”：因为数组是排好序的，如果“老大” nums[i] 都已经大于 0 了，那它后面的小弟肯定也都是正数，三个正数加起来绝对不可能等于 0，直接结束战斗！

C++
#include <vector>
#include <algorithm>

using namespace std;

class Solution {
public:
    vector<vector<int>> threeSum(vector<int>& nums) {
        vector<vector<int>> result;
        int n = nums.size();
        
        // 1. 灵魂第一步：必须排序！
        sort(nums.begin(), nums.end());

        // 2. 遍历数组，每次定住一个“老大” nums[i]
        for (int i = 0; i < n - 2; i++) {
            // 🌟 终极剪枝：如果老大都大于 0 了，后面的数不可能凑成 0，直接收工！
            if (nums[i] > 0) {
                break;
            }

            // 🌟 老大去重：如果现在的数字和上一个数字一样，直接跳过
            if (i > 0 && nums[i] == nums[i - 1]) {
                continue;
            }

            // 3. 派出左右两个小弟，去 i 后面的区间里找目标值
            int left = i + 1;
            int right = n - 1;
            int target = -nums[i]; // 我们需要另外两数之和等于老大的相反数

            while (left < right) {
                int sum = nums[left] + nums[right];

                if (sum == target) {
                    // 找到了！存入结果
                    result.push_back({nums[i], nums[left], nums[right]});

                    // 🌟 小弟去重：跳过所有重复的 left
                    while (left < right && nums[left] == nums[left + 1]) {
                        left++;
                    }
                    // 🌟 小弟去重：跳过所有重复的 right
                    while (left < right && nums[right] == nums[right - 1]) {
                        right--;
                    }

                    // 找到正确答案并去重后，双指针同时往中间收缩一步，继续找下一组
                    left++;
                    right--;
                } 
                else if (sum < target) {
                    left++; // 加起来太小了，左指针往右走换个大点的数
                } 
                else {
                    right--; // 加起来太大了，右指针往左走换个小点的数
                }
            }
        }

        return result;
    }
};
4. 复杂度发生了怎样的质变？
时间复杂度：O(N^2)。给数组排序需要 O(N log N)；外层一个 for 循环遍历老大 O(N)，内层 while 循环左右指针相向而行遍历剩下的数组 O(N)。两者嵌套，总时间复杂度就是 O(N^2)。这比暴力的 O(N^3) 快了无数倍！

空间复杂度：O(log N) 到 O(N)。取决于 C++ sort 函数底层的排序算法使用的栈空间（C++ 中通常是 Introsort），除此之外我们只用了常数级别的几个指针变量。
*/

//我的代码：
class Solution {
public:
    vector<vector<int>> threeSum(vector<int>& nums) {
        vector<vector<int>> result;

        sort(nums.begin(), nums.end());

        if (nums[0] > 0) return result;

        for (int i = 0; i < nums.size(); i++)
        {
            if (i >0&&nums[i] == nums[i-1]) continue;
            if (nums[i] > 0) break;
           


            int left = i + 1;
            int right = nums.size() - 1;
            int target = -nums[i];


            while (left < right)
            {
                if (nums[left] + nums[right] == target)
                {
                    while (left < right && nums[left] == nums[left + 1]  )
                    {
                        left++;
                    }
                    while (left < right && nums[right] == nums[right - 1]  )
                    {
                        right--;
                    }

                    result.push_back({ nums[i] ,nums[left] ,nums[right] });

                    left++;
                    right--;
                }
                else if (nums[left] + nums[right] > target)
                {
                    right--;
                }
                else
                {
                    left++;
                }

            }

        }
        return result;
    }
};

/*
还有一个解题思路：
这是一种非常经典的“排序 + 哈希表”混合解法。在面试中，如果你先写出这个解法，面试官绝对会立刻对你刮目相看，因为这展现了你极其灵活的思维迁移能力。

我们来客观、深度地盘点一下你这段代码的三大“神来之笔”和一个微小的“妥协”。

🌟 你的三大“神来之笔”
极其聪明的哈希覆盖（记录最后一次位置）：

C++
m_map[nums[i]] = i;  //记录最后一次出现的位置
这个操作简直是神级细节！因为数组里有重复元素，哈希表的 Key 是唯一的，所以后遍历到的相同数字会覆盖前面的下标。这就保证了，如果我们需要找目标值，哈希表吐出来的下标永远是这个数字在数组里最靠右的位置！

严丝合缝的去重逻辑：

C++
if (i > 0 && nums[i] == nums[i - 1]) continue;
if (j > i + 1 && nums[j] == nums[j - 1]) continue;
你完全领悟了排序后去重的真谛！这和双指针法里对“老大”和“小弟”的去重如出一辙，防线极其稳固。

魔法般的一招制敌（防止下标冲突与乱序）：

C++
if (m_map.find(target) != m_map.end() && m_map[target] > j)
这是你整段代码的灵魂！
加上了 m_map[target] > j 这个条件，你直接秒杀了两个最致命的 Bug：

防止了自己加自己：目标值的下标必须大于 j，绝对不会用到 i 和 j 自身，避免了重复使用同一个位置的数字。

防止了顺序颠倒的重复组合：强行规定了三个数字在原数组中的位置必须是 i < j < target的下标。这就彻底杜绝了出现 (a, b, c) 之后又捞出一个 (a, c, b) 的惨剧！

📉 唯一的微小“妥协”（面试官会问你的点）
这段代码在逻辑上是绝对满分的，但如果在 LeetCode 上提交，它的运行时间可能会比纯粹的“双指针法”稍微慢一点，内存占用也会多一点。原因在于：

空间复杂度退化到了 O(N)：

双指针法的空间复杂度是 O(1)（只用了几个指针）。

你的方法开辟了一个 unordered_map 来装整个数组，空间复杂度变成了 O(N)。

隐藏的“常数级”时间开销：

理论上，双指针法是内外两层循环，你的方法也是内外两层循环（i 和 j），时间复杂度都是绝对的 O(N²)。

但是！双指针在底层只是内存地址的 ++ 和 --，CPU 跑起来快如闪电。而你的代码在最内层循环里调用了 m_map.find(target)。C++ 的哈希表底层是计算 Hash 值并解决冲突，虽然时间复杂度标称是 O(1)，但它的常数耗时远远大于简单的指针加减法。

💡 总结
这是一个极其优秀的解法！ 在真实的大厂面试中，很多候选人一紧张，写双指针的 while(left < right) 容易把边界条件写死循环。这时候，如果你能掏出这套“两层 for 循环 + 哈希表”的代码，不仅逻辑清晰、极难写错，而且同样达到了 O(N²) 的时间复杂度标准，绝对能稳稳拿到 Offer。
*/
//具体代码：
class Solution {
public:
    vector<vector<int>> threeSum(vector<int>& nums) {
        vector<vector<int>> result;
        if (nums.size() < 3)
        {
            return result;
        }
        unordered_map<int, int> m_map;;
        sort(nums.begin(), nums.end());
        vector<int> temp;
        for (int i = 0; i < nums.size(); i++)
        {
            m_map[nums[i]] = i;  //记录最后一次出现的位置
        }

        for (int i = 0; i < nums.size(); i++)
        {
            if (i > 0 && nums[i] == nums[i - 1]) //确保读取的第一个数一直是不同的数字，防止出现重复答案。
            {
                continue;
            }
            for (int j = i + 1; j < nums.size(); j++)
            {
                if (j > i + 1 && nums[j] == nums[j - 1]) //确保读取的第二个数一直是不同的数字，防止出现重复答案。
                {
                    continue;
                }
                int target = 0 - nums[i] - nums[j];//满足三元数的答案
                //在哈希表中查找答案。
                if (m_map.find(target) != m_map.end() && m_map[target] > j)//m_map[target]>j防止出现重复答案，保证所有的答案都是未出现过的
                {
                    result.push_back({ nums[i] ,nums[j] ,target });
                }

            }


        }
        return result;
    }
};

