/*
解题思路：
Gemini 说
这道题（LeetCode 152. 乘积最大子数组）是动态规划里非常经典的一道“双面人”问题。

如果你做过它的兄弟题“最大子数组和”（Kadane 算法），你可能会觉得这题很简单：无非就是把加法换成乘法嘛！
大错特错！乘法里藏着一个致命的“恶魔”：负负得正。

1. 核心思维：防备“绝地反击”的负数
在求“和”的时候，一个极小的负数（比如 -100）就是个纯纯的累赘，我们巴不得赶紧把它丢掉。
但在求“乘积”的时候，情况变了！一个极小的负数（-100），如果它能在后面再遇到一个负数（比如 -10），它俩一相乘，瞬间就会发生爆炸级的“绝地反击”，变成 +1000！

这就引出了这道题的灵魂逻辑：
我们不仅要记录“当前的最大乘积”，我们还要时刻保留着“当前的最小乘积（绝对值极大的负数）”。因为这个最小乘积，随时可能因为乘以一个负数，瞬间翻身成为全场的最大乘积！

2. 状态转移方程（极简的“乾坤大挪移”）
基于上面的思路，我们需要维护两个状态变量：

max_prod：以当前数字结尾的，能凑出的最大乘积。

min_prod：以当前数字结尾的，能凑出的最小乘积。

当我们遍历到一个新数字 nums[i] 时，会发生什么？

如果 nums[i] 是正数：大家相安无事。max_prod 乘以它会变得更大，min_prod 乘以它依然很小（或变得更小）。

如果 nums[i] 是负数：乾坤大挪移来了！原来的最大值乘上负数，变成了最小值；原来的最小值乘上负数，变成了最大值！

所以，遇到负数时，我们只需要直接把 max_prod 和 min_prod 交换一下，然后再继续乘，逻辑就又完美闭环了！

怎么决定是跟前面的连起来，还是自己另起炉灶？
依然是老套路，我们让 nums[i] 和 前面的连乘结果 打一架，取大的：

更新最大值：max_prod = max(nums[i], max_prod * nums[i])

更新最小值：min_prod = min(nums[i], min_prod * nums[i])

3. 手工推演：nums = [2, 3, -2, 4, -1]
初始状态：ans = 2, max_prod = 2, min_prod = 2

走到 3（正数，不交换）：

max_prod = max(3, 2 * 3) = 6

min_prod = min(3, 2 * 3) = 3

ans 刷新为 6。

走到 -2（负数，触发交换！ 现在 max=3, min=6）：

max_prod = max(-2, 3 * -2) = -2 （因为乘了负数，最大值萎缩了）

min_prod = min(-2, 6 * -2) = -12 （保留了一颗极其强大的负数炸弹）

ans 依然是 6。

走到 4（正数，不交换）：

max_prod = max(4, -2 * 4) = 4 （前面是负的，4 选择另起炉灶，不跟前面乘了）

min_prod = min(4, -12 * 4) = -48 （炸弹变得更大了）

ans 依然是 6。

走到 -1（负数，触发交换！ 现在 max=-48, min=4）：

max_prod = max(-1, -48 * -1) = 48 （💥 见证奇迹！-48 碰到 -1，瞬间反杀变成最大值！）

min_prod = min(-1, 4 * -1) = -4

ans 刷新为 48。

最终答案就是 48（来自于 [2, 3, -2, 4, -1] 全部相乘）！


*/

//ai的代码
class Solution {
public:
    int maxProduct(vector<int>& nums) {
        if (nums.empty()) return 0;

        // 初始化：以第 0 个元素为基准
        int max_prod = nums[0];
        int min_prod = nums[0];
        int ans = nums[0];

        // 从第 1 个元素开始遍历
        for (int i = 1; i < nums.size(); i++) {
            // 遇到负数，最大值和最小值互换身份
            if (nums[i] < 0) {
                swap(max_prod, min_prod);
            }

            // 更新 max_prod 和 min_prod
            // 要么自己单干，要么和前面的接龙
            max_prod = max(nums[i], max_prod * nums[i]);
            min_prod = min(nums[i], min_prod * nums[i]);

            // 顺手记录历史最高票房
            ans = max(ans, max_prod);
        }

        return ans;
    }
};
//我的代码
class Solution {
public:
    int maxProduct(vector<int>& nums) {
        if (nums.empty()) return 0;

        int max_cheng = nums[0];
        int min_cheng = nums[0];
        int ans = nums[0];


        for (int i =1; i < nums.size(); i++)
        {
            if (nums[i] < 0)
            {
                swap(max_cheng,min_cheng);
            }

            max_cheng = max(nums[i], nums[i]* max_cheng);
            min_cheng = min(nums[i], nums[i]* min_cheng);

            ans = max(ans, max_cheng);
        }

        return ans;

    }
};
