/*
这道题（LeetCode 169. 多数元素）是算法界又一个极其巧妙的“脑筋急转弯”。

虽然这道题用哈希表（记仇本）或者排序（排好队取中间那个）都能轻松做出来，但面试官真正在等的，是一个时间复杂度 O(N)、空间复杂度绝对 O(1) 的神级解法——摩尔投票法（Boyer-Moore Voting Algorithm）。

它的思想和上一道题的“异或湮灭”有异曲同工之妙，只不过这次变成了“阵营大乱斗”。

1. 核心思维：同归于尽的“擂台赛”
题目给了一个极其关键的条件：多数元素的出现次数大于 ⌊n/2⌋。
这意味着什么？意味着这个“多数派”的人数，比其他所有“少数派”的人数加起来还要多！

游戏规则极其残酷（一换一）：
我们把数组里的每一个数字想象成不同阵营的士兵。

我们设立一个擂台，选第一个走过来的士兵当“擂主”（candidate），初始血量（count）为 1。

接下来每过来一个士兵：

如果是自己人（数字和擂主一样）：上台支援，擂主血量 +1。

如果是敌人（数字和擂主不一样）：直接在台下跟擂主“一换一”同归于尽，擂主血量 -1。

如果擂主血量掉到 0：说明台上的阵营拼光了。下一个走过来的士兵直接白捡一个擂主，成为新的 candidate，血量重新记为 1。

为什么最后站在台上的肯定是“多数派”？
因为多数派的人数超过了一半啊！就算最极端的死法——其他所有阵营的人全都联合起来，排着队去跟多数派“一换一”，多数派最后也至少还能剩下 1 个人稳稳站在擂台上！

2. 手工推演：nums = [2, 2, 1, 1, 1, 2, 2]
准备两个变量：candidate（擂主）和 count（血量）。初始都是 0。

走到 2：擂台空着（count == 0）。2 当擂主。candidate = 2, count = 1。

走到 2：自己人！血量增加。candidate = 2, count = 2。

走到 1：敌人！一换一。血量减少。candidate = 2, count = 1。

走到 1：敌人！一换一。血量减少。candidate = 2, count = 0。（擂主 2 被拼光了！）

走到 1：擂台空着（count == 0）。1 趁机当擂主。candidate = 1, count = 1。

走到 2：敌人！一换一。血量减少。candidate = 1, count = 0。（擂主 1 被拼光了！）

走到 2：擂台空着（count == 0）。2 当擂主。candidate = 2, count = 1。

数组走完，最后站在擂台上的 candidate 是 2。它就是绝对的多数派！

3. C++ 满分极客代码
把上面的“擂台赛”逻辑翻译成代码，你会发现它比想象中还要短小精悍：

C++
#include <vector>

using namespace std;

class Solution {
public:
    int majorityElement(vector<int>& nums) {
        int candidate = 0; // 记录当前的擂主
        int count = 0;     // 记录擂主的血量（阵营优势）

        for (int num : nums) {
            // 如果擂台上没人了，当前士兵直接占领擂台
            if (count == 0) {
                candidate = num;
            }

            // 如果过来的士兵和擂主是一个阵营的，血量 +1
            // 如果不是一个阵营的，直接一换一同归于尽，血量 -1
            if (num == candidate) {
                count++;
            } else {
                count--;
            }
        }

        // 活到最后的那个阵营，就是我们要找的多数派
        return candidate;
    }
};
4. 复杂度分析
时间复杂度：O(N)。就像“只出现一次的数字”那样，我们只把数组从头到尾遍历了一次，每个人上台打一架就结束了。

空间复杂度：O(1)。没有使用哈希表，只用了 candidate 和 count 两个变量记录状态，空间开销被压榨到了极限。

顿悟时刻
这种“极限一换一”的思想在处理数据流统计时极其霸道。只要你占据了绝对优势（超过 50%），无论敌人怎么挣扎，笑到最后的一定是你。

既然你已经掌握了“超过一半（⌊n/2⌋）”的摩尔投票法，LeetCode 立刻就有一道它的亲兄弟进阶版等着你：

*/

//我的代码：
class Solution {
public:
    int majorityElement(vector<int>& nums) {
        int leizhu = 0;
        int xieliang = 0;

        for (int num : nums)
        {
            if (xieliang == 0)
            {
                leizhu = num;
                xieliang = 1;
            }
            else
            {
                if (num == leizhu)
                {
                    xieliang++;
                }
                else
                {
                    xieliang--;
                }
            }
        }
        return leizhu;
    }
};
