/*
之前在“环形链表”里，我们利用的是乌龟和兔子的“速度差”（一个走1步，一个走2步）。
而在这道题（LeetCode 19. 删除链表的倒数第 N 个结点）里，我们要利用的是极致的“起跑时间差”（速度一样，但让一个人先跑）！

如果在面试中，你先遍历一遍数出链表总长度 L，然后再从头走 L−N 步去删除。面试官会点点头，但紧接着就会抛出那个经典的灵魂拷问：
“你能只遍历一次链表（One Pass）就把它删掉吗？”

在不知道终点在哪里的情况下，只走一次就精准狙击倒数第 N 个节点，诀窍就是四个字：“保持车距”！

1. 核心思维：用“物理尺子”丈量链表
我们可以把两个指针（fast 和 slow）想象成一把长度固定为 N 的“物理尺子”的两端。

绝杀三步曲：

拉开间距（兔子先跑）：让兔子 fast 先往前狂奔 N 步。此时，兔子和站在起点的乌龟 slow 之间，刚好隔了 N 个节点的距离。

同速并排跑：现在，乌龟和兔子以相同的速度（每次各走1步）一起往前走。因为他们的速度一样，所以他们之间的“车距”永远保持为 N。

坠崖即中点：当兔子 fast 走到链表尽头（悬崖边）时，乌龟 slow 在哪里？
因为它比兔子落后 N 步，所以乌龟绝对、刚好、一分不差地停在了倒数第 N+1 个节点上（也就是要删除的那个节点的正前方）！

🚨 为什么乌龟要停在正前方？
你前面学“反转链表”和“合并链表”时已经深刻体会到了：在链表里，要想删除或者操作一个节点，你绝对不能站在它身上！你必须站在它的前驱节点（前一个节点）上，才能通过修改 next 指针把它踢出局！

2. 致命防御：如果被删的是“头节点”怎么办？
假设链表长度就是 N，也就是我们要删除的是原链表的第一个节点。
这时候兔子先跑 N 步，直接就掉下悬崖了。乌龟还在原地，根本没法操作乌龟的“前一个节点”（因为乌龟前面没路了）。

破局战术：请出上节课的绝对真神 —— 虚拟头节点（Dummy Node）！
只要我们在原链表前面挂一个 dummy，让乌龟和兔子一开始都站在 dummy 上。
这样就算要把原先的 head 删掉，乌龟也有 dummy 这个落脚点，直接执行 dummy->next = dummy->next->next 即可安全斩首！

3. C++ 满分工业级代码（极致丝滑）
这段代码将“无敌哨兵”和“保持车距”结合到了极致。如果你能在面试的白板上徒手写出这段带有 delete 内存管理的 C++ 代码，绝对能征服面试官：

C++
/**
 * Definition for singly-linked list.
 * struct ListNode {
 * int val;
 * ListNode *next;
 * ListNode() : val(0), next(nullptr) {}
 * ListNode(int x) : val(x), next(nullptr) {}
 * ListNode(int x, ListNode *next) : val(x), next(next) {}
 * };
 */
class Solution {
public:
    ListNode* removeNthFromEnd(ListNode* head, int n) {
        // 1. 请出无敌哨兵，应对删除头节点的极限情况
        ListNode* dummy = new ListNode(0);
        dummy->next = head;
        
        // 乌龟和兔子一开始都站在起跑线（dummy）上
        ListNode* slow = dummy;
        ListNode* fast = dummy;

        // 2. 兔子先跑 N 步，拉开 N 的车距
        for (int i = 0; i < n; i++) {
            fast = fast->next;
        }

        // 3. 兔子和乌龟同速奔跑！
        // 当兔子走到最后一个节点时（fast->next == nullptr 悬崖勒马）
        // 乌龟刚好停在要删除节点的前一个位置！
        while (fast->next != nullptr) {
            fast = fast->next;
            slow = slow->next;
        }

        // 4. 实施精准斩首！
        // 记录一下即将被删掉的节点（为了职业素养，释放内存）
        ListNode* nodeToDelete = slow->next;
        
        // 跨过要删除的节点，直接连到下下个
        slow->next = slow->next->next;

        // 5. 收尾工作：释放被删除节点的内存，拿到真正的新头节点，释放 dummy
        delete nodeToDelete;
        ListNode* newHead = dummy->next;
        delete dummy;

        return newHead;
    }
};
4. 复杂度发生了怎样的质变？
时间复杂度：O(L)。其中 L 是链表的总长度。我们让兔子和乌龟加起来刚好把链表走了一遍（One Pass），没有回头路，达到了这道题理论上的绝对速度极限。

空间复杂度：绝对的 O(1)。除了 dummy、slow 和 fast 几个孤立的指针外，没有开辟任何多余的空间。
*/
//我的代码：
class Solution {
public:
    ListNode* removeNthFromEnd(ListNode* head, int n) {
        ListNode dummy(0);
        dummy.next = head;
        ListNode* slow = &dummy;
        ListNode* fast = &dummy;

        for (int i = 0; i < n; i++)//fast先跑n步
        {
            fast = fast->next;
        }

        while (fast != nullptr && fast->next != nullptr)//当fast->next==nullptr时结束循环
        {
            slow = slow->next;
            fast = fast->next;
        }
        ListNode* nodeToDelete = slow->next;
      
        slow->next = slow->next->next;

        delete nodeToDelete;
        return dummy.next;
    }
};






