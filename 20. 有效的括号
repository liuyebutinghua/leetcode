/*
解题的思路：
1. 核心思维：为什么用栈？
仔细观察有效括号的规则：“左括号必须以正确的顺序闭合”。
这意味着：最后出现的那一个左括号，必须最先被对应的右括号闭合。

想象你在洗盘子：

遇到一个左括号，就像把一个盘子**压入（Push）**栈中。

遇到一个右括号，就需要和栈顶（最上面）的盘子进行匹配。如果匹配成功，就把这个盘子弹出（Pop）。

如果所有的括号都能完美匹配，最后栈应该是空的。

2. 巧妙的代码技巧：存入期待的右括号
通常的想法是：遇到左括号 (，就把 ( 压入栈。遇到右括号 )，就看栈顶是不是 (。
这样写需要写很多组对称的判断，代码比较臃肿。

✨ 一个非常聪明的技巧：
当我们遇到左括号时，直接把与之对应的右括号压入栈中。

遇到 (，压入 )。

遇到 [，压入 ]。

遇到 {，压入 }。

这样一来，当我们真的遇到右括号时，只需要判断：这个右括号，和栈顶的元素相等吗？ 相等就是匹配成功！

3. 三种注定失败的“无效”情况
在遍历字符串的过程中，我们可能会遇到以下三种导致匹配失败的情况：

右括号太多了：比如 s = "())"。

遍历到最后一个 ) 时，栈已经空了，说明没有多余的左括号来匹配它了。

括号类型不匹配：比如 s = "(]"。

遍历到 ] 时，栈顶是 )，两者不相等，匹配失败。

左括号太多了：比如 s = "(()"。

字符串全都遍历完了，栈里还剩下一个 ) 没被匹配掉（栈不为空）。
*/

//我的代码
class Solution {
public:
    bool isValid(string s) {
        if (s.size() % 2 != 0) return false;

        stack<char> st;

        for (char c : s)
        {
            if (c == '(')
            {
                st.push(')');
            }
            else if (c == '[')
            {
                st.push(']');
            }
            else if (c == '{')
            {
                st.push('}');
            }
            else
            {
                if (st.empty() || c != st.top())
                {
                    return false;
                }

                st.pop();

            }



        }

        return st.empty();

    }
};

//ai的代码


class Solution {
public:
    bool isValid(string s) {
        // 如果字符串长度是奇数，绝不可能有效，直接剪枝（提速小技巧）
        if (s.size() % 2 != 0) return false;

        stack<char> st;

        for (char c : s) {
            // 1. 遇到左括号，就把期待的右括号压入栈中
            if (c == '(') {
                st.push(')');
            }
            else if (c == '{') {
                st.push('}');
            }
            else if (c == '[') {
                st.push(']');
            }
            // 2. 遇到右括号了
            else {
                // 如果栈空了（右括号太多） 
                // 或者 栈顶元素和当前遇到的右括号不相等（类型不匹配）
                if (st.empty() || st.top() != c) {
                    return false;
                }
                // 匹配成功，把栈顶元素弹出来
                st.pop();
            }
        }

        // 3. 遍历结束后，如果栈是空的，说明所有括号都完美匹配了
        // 如果栈不为空（比如 "(((" ），返回 false
        return st.empty();
    }
};


