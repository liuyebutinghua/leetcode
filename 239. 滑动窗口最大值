/*
你直接硬刚到了滑动窗口领域的绝对巅峰、LeetCode 最经典的 Hard 题之一：239. 滑动窗口最大值。

之前我们在做“定长滑动窗口”（找异位词）时，只维护了字母的频次；但在这道题里，每次窗口滑动，你都要知道当前窗口里的最大值是谁。

如果每次滑动都去遍历那 k 个数字找最大值，时间复杂度会退化成 O(N×k)，在 Hard 题的巨量测试用例下绝对会超时。

这道题的唯一真神解法，是引入一个极其霸道、充满“职场丛林法则”的高级数据结构：单调队列（Monotonic Queue）！

1. 核心思维：职场丛林法则（单调队列）
为了在 O(N) 的时间里搞定，我们需要一个双端队列（deque）来当“人才储备库”。
这个队列里存的是数字的下标（为了方便判断他们有没有“退休/过期”）。

队列的绝对铁律（丛林法则）：
把窗口滑动想象成时间的流逝，新数字就像刚毕业的年轻人，老数字就像即将退休的老员工。数字的大小代表他们的“业务能力”。

“如果一个年轻人比你强，还比你年轻，那你就彻底没用了！”
当一个新数字准备进队列时，如果它比队尾的那些老员工大（能力强），而且因为它后进来，它在窗口里存活的时间肯定比老员工长（年轻）。那么前面那些又老又弱的员工就可以直接开除（从队尾 pop 掉），因为他们这辈子都不可能成为最大值了！

“留着做备胎”
如果新进来的年轻人比队尾的老员工弱，怎么办？不能开除他！虽然他现在不是老大，但等前面的大佬们都“退休”了，山中无老虎，他就有可能猴子称大王。所以老老实实排在队尾。

“到点退休”
每次窗口滑动，都要检查队头的老大。如果老大的下标已经跌出了当前窗口的范围，说明他退休了，直接从队头请走（pop_front）。

在这套残酷的法则下，队列里的数字永远是从大到小严格递减的（单调递减）。而队头永远是当前窗口的最强者（最大值）！

2. 手工推演：nums = [1, 3, -1, -3, 5, 3], k = 3
准备一个双端队列 dq（存下标），结果数组 result。

i = 0 (进 1)：队列空，1 入队。dq = [0] (对应值 [1])

i = 1 (进 3)：新来的 3 比队尾的 1 强！1 被无情开除。3 入队。dq = [1] (对应值 [3])

i = 2 (进 -1)：新来的 -1 没 3 强，留下当备胎。dq = [1, 2] (对应值 [3, -1])

👉 窗口形成 (大小为 3)：队头永远是老大。记录答案：3。

i = 3 (进 -3)：-3 没 -1 强，当备胎的备胎。dq = [1, 2, 3] (对应值 [3, -1, -3])

👉 窗口移动：队头 1 (值 3) 还在窗口内。记录答案：3。

i = 4 (进 5)：血洗职场！ 来的 5 太强了！比 -3 强，删！比 -1 强，删！比 3 强，删！全部清空，5 登基。dq = [4] (对应值 [5])

👉 窗口移动：记录答案：5。

i = 5 (进 3)：3 没 5 强，当备胎。dq = [4, 5] (对应值 [5, 3])

👉 窗口移动：记录答案：5。

最终输出：[3, 3, 5, 5]。完美！

3. C++ 满分工业级代码
在 C++ 中，我们直接使用 std::deque（双端队列），可以在头尾两端进行极速的 O(1) 插入和删除。

C++
#include <vector>
#include <deque>

using namespace std;

class Solution {
public:
    vector<int> maxSlidingWindow(vector<int>& nums, int k) {
        vector<int> result;
        deque<int> dq; // 双端队列，存储数组的【下标】

        for (int i = 0; i < nums.size(); i++) {
            // 1. 淘汰过期大佬：如果队头的下标已经不在窗口 [i - k + 1, i] 内，让他退休
            if (!dq.empty() && dq.front() < i - k + 1) {
                dq.pop_front();
            }

            // 2. 职场丛林法则：如果新来的年轻人比队尾的员工强，把队尾统统开除
            while (!dq.empty() && nums[dq.back()] <= nums[i]) {
                dq.pop_back();
            }

            // 3. 新人入列：把当前元素的下标加到队尾
            dq.push_back(i);

            // 4. 记录答案：只有当窗口完全形成后（也就是遍历到第 k-1 个元素及以后）才开始记录
            if (i >= k - 1) {
                // 队头永远是当前窗口的最强者
                result.push_back(nums[dq.front()]);
            }
        }

        return result;
    }
};
4. 🚨 灵魂拷问：这真的是 O(N) 吗？
看着那个 for 循环里套着一个 while 循环，面试官一定会挑刺：“你确定这没有退化成 O(N 
2
 ) 吗？”

这时候你要用极其笃定的语气回答：
“绝对是 O(N)！因为对于数组里的每一个元素，它最多只会被 push_back 进队列一次，也最多只会被 pop_back 或者 pop_front 弹出一次。”
既然每个元素进出队列的次数都是常数次（最多 1 进 1 出），那摊销下来的时间复杂度就是绝对的线性的 O(N)！

🏆 顿悟时刻
你看，“单调队列”就是为了这种“求滑动窗口最值”而生的神仙兵器。只要你领悟了“比你年轻还比你强，你就被淘汰了”这句话，这代码你闭着眼睛都能默写出来。
*/


//我的代码：
class Solution {
public:
    vector<int> maxSlidingWindow(vector<int>& nums, int k) {
        vector<int> result;
        
        deque<int> dp;

        for (int i = 0; i < nums.size(); i++)
        {
            if (!dp.empty() && i - dp.front() >= k)//大佬退休
            {
                dp.pop_front();
            }

            while (!dp.empty() && nums[i] > nums[dp.back()])//新人入职前，干掉比他弱的
            {
                dp.pop_back();
            }

            dp.push_back(i);//新人入职

            if (i - k + 1 >= 0)//入职人数超过k后（不是实际工作人数），开始记录最强者
            {
                result.push_back(nums[dp.front()]);
            }
            
        }

        return result;
    }
};
