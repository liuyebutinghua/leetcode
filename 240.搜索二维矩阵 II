/*
很多人看到“排好序”，第一反应就是每行单独做一个二分查找，时间复杂度是 O(M log N)。面试官会对这个解法点点头，但紧接着就会问：“你能把时间复杂度降到 O(M + N) 吗？”

要在 O(M + N) 的时间里找到目标，我们必须利用这个矩阵极其特殊的“地形”！

1. 核心思维：把矩阵看成一棵“二叉搜索树” (BST)
这道题最核心的破局点，在于“你一开始站在哪”。

如果你站在左上角（最小值），往右走变大，往下走也变大。如果目标值比你大，你根本不知道该往右走还是往下走。右下角（最大值）同理。

但是，如果你站在“右上角”呢？
右上角的数字有一个极其魔幻的属性：

它是这一行里的最大值（往左走，数字变小）。

它是这一列里的最小值（往下走，数字变大）。

这不就是一棵天然的二叉搜索树吗！
以右上角的数字为根节点：

想找更小的数？向左走（相当于二叉树的左子树）。

想找更大的数？向下走（相当于二叉树的右子树）。

2. 手工推演：化身“无情刺客”
假设我们要找 target = 5，矩阵如下：

Plaintext
1   4   7  11
2   5   8  12
3   6   9  16
我们直接空降到右上角，站在 11 的位置：

当前是 11：11 > 5。目标太小了，这一列下面的数字比 11 还要大，绝对不可能有 5。果断往左走，整列淘汰！

当前是 7：7 > 5。还是太大，下面全比 7 大。往左走，淘汰整列！

当前是 4：4 < 5。目标比较大，这一行左边的数字比 4 还要小，绝对不可能有 5。果断往下走，整行淘汰！

当前是 5：5 == 5。精准命中，直接返回 true！

这就是传说中的“Z 字形查找”（或者叫削边法）。每次比较，都能极其霸道地淘汰掉“一整行”或“一整列”！

3. C++ 满分极客代码
逻辑清晰，代码极简，只要找准了右上角（或者左下角），这道题直接秒杀：

C++
#include <vector>

using namespace std;

class Solution {
public:
    bool searchMatrix(vector<vector<int>>& matrix, int target) {
        if (matrix.empty() || matrix[0].empty()) return false;

        int m = matrix.size();
        int n = matrix[0].size();

        // 1. 空降右上角
        int row = 0;
        int col = n - 1;

        // 2. 只要没有走出矩阵边界，就继续找
        while (row < m && col >= 0) {
            if (matrix[row][col] == target) {
                return true; // 找到了！
            } 
            else if (matrix[row][col] > target) {
                col--; // 当前值太大，向左走找更小的
            } 
            else {
                row++; // 当前值太小，向下走找更大的
            }
        }

        // 走到边界都没找到，说明矩阵里没有
        return false;
    }
};
4. 复杂度发生了怎样的质变？
时间复杂度：O(M + N)。在最坏的情况下，你从右上角一路走到左下角，最多往下走 M 步，往左走 N 步。所以总步数绝对不会超过 M + N。这比每一行做二分查找的 O(M log N) 要快得多！

空间复杂度：O(1)。只用了 row 和 col 两个变量记录坐标，零额外空间开销。

*/

//我的代码：
class Solution {
public:
    bool searchMatrix(vector<vector<int>>& matrix, int target) {
        if (matrix.empty() || matrix[0].empty()) return false;

        int m = matrix.size();
        int n = matrix[0].size();

        int row = 0;
        int col = n - 1;

        while (row >= 0 && row < m && col >= 0 && col < n)
        {
            if (matrix[row][col] == target)
            {
                return true;
            }
            else if (matrix[row][col] > target)
            {
                col--;
            }
            else
            {
                row++;
            }

        }

        return false;


    }
};


/*
新的需求：现在题目升级了，不仅要问你“最长有多长”，还要你“把人交出来”（输出具体的序列）。


解题思路：
这就好比你不仅要知道迷宫最短路径是几步，还要画出寻路地图。在动态规划中，应对这种需求有一个万能的经典套路：“沿途撒面包屑（记录前驱节点）”。
1. 核心思维：用 pre 数组记录“你是从哪来的？”在你原来的代码中，当 nums[i] > nums[j] 并且 dp[j] + 1 > dp[i] 时，
你更新了 dp[i]。这在物理上的意义是：“为了让队伍最长，我（节点 $i$）决定跟在节点 $j$ 的后面！”既然如此，
我们只需要多准备一个叫做 pre (前驱) 的数组。每次做决定的那一刻，顺手在 pre[i] 里记下 $j$ 的下标：
pre[i] = j。等到整个 DP 遍历结束，我们先找到全场 dp 值最大的那个人的下标（终点），然后顺着 pre 数组一路往回找，
就能拔出萝卜带出泥，把整支队伍全扯出来！2. C++ 满分改造代码我们在你的代码基础上，只加了三个东西：pre 数组、
记录最大值的 max_index，以及最后的逆推循环。

C++class Solution {
public:
    vector<int> getLIS(vector<int>& nums) {
        int n = nums.size();
        if (n == 0) return {};
        if (n == 1) return {nums[0]};

        vector<int> dp(n, 1);
        vector<int> pre(n, -1); // 🌟 核心道具：备忘录，初始化为 -1 表示没有前驱

        int max_length = 1;
        int max_index = 0;      // 🌟 记录最长序列的终点究竟是哪个人

        for (int i = 0; i < n; i++) {
            for (int j = 0; j < i; j++) {
                if (nums[i] > nums[j]) {
                    // 如果跟在 j 后面能让队伍变得更长
                    if (dp[j] + 1 > dp[i]) {
                        dp[i] = dp[j] + 1;
                        pre[i] = j; // 🌟 记下恩人：我 i 决定跟在 j 后面！
                    }
                }
            }

            // 更新全局最大值时，同步记录这个最大值是落在哪个下标上的
            if (dp[i] > max_length) {
                max_length = dp[i];
                max_index = i; 
            }
        }

        // 🌟 拔出萝卜带出泥：从终点开始顺藤摸瓜
        vector<int> result;
        int curr = max_index;
        while (curr != -1) {
            result.push_back(nums[curr]); // 把当前人拉进队伍
            curr = pre[curr];             // 顺着线索去找上一个人
        }

        // 因为我们是从后往前找的，所以队伍是倒着的，需要翻转一下
        reverse(result.begin(), result.end());

        return result;
    }
};
3. 破案回放：追踪前驱节点为了让你对这个“顺藤摸瓜”的过程有极其直观的肌肉记忆，
我为你准备了一个交互式的动态模拟器。你可以点击“下一步”，
亲眼看看程序是如何在寻找最大长度的同时，悄悄用 pre 数组画出箭头的；
最后又是如何顺着箭头把序列揪出来的。

*/



