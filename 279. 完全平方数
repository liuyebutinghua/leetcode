/*
解题思路：
Gemini 说
这道题（LeetCode 279. 完全平方数）可以说是动态规划（DP）家族里“背包问题”的绝佳敲门砖！

如果你回想一下刚才做过的“爬楼梯”，你会发现这两道题的底层逻辑惊人地相似。
在“爬楼梯”里，你每次只能跨 1 步 或 2 步。
而在这道“完全平方数”里，你可以把数字 n 看作是楼梯的总台阶数。你每次能跨的步数，变成了完全平方数（1步、4步、9步、16步...）。题目问的是：最少跨几次能正好到达楼顶？

1. 核心思维：拆解目标（找子问题）
假设我们要算 n=12 时的最少数量。
比 12 小的完全平方数有：1, 4, 9。

那么拼凑出 12 的最后一步，必然是这三种情况之一：

最后加了一个 1：那前面的总和必然是 11。总数量 = 拼出 11 的最少数量 + 1。

最后加了一个 4：那前面的总和必然是 8。总数量 = 拼出 8 的最少数量 + 1。

最后加了一个 9：那前面的总和必然是 3。总数量 = 拼出 3 的最少数量 + 1。

作为极其理性的算法，我们当然是把这三种情况都算出来，然后挑一个最小的！

这就是这道题的状态转移方程：

dp[i]=min(dp[i],dp[i−j×j]+1)
（其中 j×j≤i，代表我们尝试减去每一个可能的完全平方数）

2. 手工推演：一步步填满 DP 数组
我们定义 dp[i] 表示：和为 i 的完全平方数的最少数量。

基础状态：dp[0] = 0。凑出 0 不需要任何数字。

最坏的情况是什么？就是全用 1 来凑。比如凑 5，最坏就是 1+1+1+1+1，需要 5 个。所以初始时，我们可以把 dp[i] 的默认值设为 i。

我们从 1 开始推到 12：

dp[1]：尝试减去 1²。dp[1] = dp[1-1] + 1 = dp[0] + 1 = 1。

dp[2]：尝试减去 1²。dp[2] = dp[2-1] + 1 = dp[1] + 1 = 2。

dp[3]：尝试减去 1²。dp[3] = dp[3-1] + 1 = dp[2] + 1 = 3。

dp[4]：可以减去 1²，也可以减去 2²(也就是4)。

选 1²：dp[4-1] + 1 = dp[3] + 1 = 4

选 2²：dp[4-4] + 1 = dp[0] + 1 = 1

挑最小的！所以 dp[4] = 1。

照这个逻辑一直推到 12，所有的答案就都浮出水面了


*/

//ai的代码
#include <vector>
#include <algorithm>

using namespace std;

class Solution {
public:
    int numSquares(int n) {
        // 创建 dp 数组，大小为 n + 1
        // 初始时把每个位置的值设为它本身（最坏情况：全用 1 凑）
        vector<int> dp(n + 1);
        for (int i = 0; i <= n; i++) {
            dp[i] = i; 
        }

        // 外层循环：逐步求出 dp[1], dp[2] ... 一直到 dp[n]
        for (int i = 1; i <= n; i++) {
            // 内层循环：尝试减去所有比 i 小的完全平方数 (1, 4, 9, 16...)
            // 条件是 j * j <= i
            for (int j = 1; j * j <= i; j++) {
                // 状态转移方程：看看减去 j*j 后，需不需要更少的数字
                dp[i] = min(dp[i], dp[i - j * j] + 1);
            }
        }

        return dp[n];
    }
};

//我的代码
class Solution {
public:
    int numSquares(int n) {
        vector<int> dp(n + 1);

        for (int i = 0; i <= n; i++)
        {
            dp[i] = i;
        }

        for (int i = 1; i <= n; i++)
        {

            for (int j = 1; j * j <= i; j++)
            {
                dp[i] = min(dp[i],dp[i-j*j]+1);
            }

        }

        return  dp[n];

    }
};
