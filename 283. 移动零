/*
这道题（LeetCode 283. 移动零）是“双指针（Two Pointers）”家族里最基础、也最经典的“快慢指针”模型。你之前在“颜色分类（荷兰国旗）”里已经玩过更复杂的三指针了，所以这道题对你来说绝对是秒懂。

1. 核心思维：快慢指针的“填坑”游戏
题目要求：把所有的 0 挪到最后，并且非零元素的相对顺序不能乱，还要原地修改。

我们可以安排一快一慢两个指针，让它们从头开始往右跑：

快指针（fast，侦察兵）：它的任务只有一个，就是一往无前地遍历整个数组，去寻找非零的数字。

慢指针（slow，值日生）：它永远指着“下一个准备存放非零数字的安全空位”。

干活的逻辑极其简单：
当快指针 fast 遇到 0 的时候，不要管它，直接跳过往前走（因为 0 最终要被发配到边疆去）。
当快指针 fast 遇到非零数字的时候，太好了！直接把这个数字和慢指针 slow 指着的位置交换（swap）！交换完之后，慢指针 slow 往前挪一步，准备迎接下一个非零数字。

2. 手工推演：nums = [0, 1, 0, 3, 12]
初始状态：slow = 0, fast = 0。

fast = 0（指向 0）：侦察兵看到 0，无视它，fast++。

fast = 1（指向 1）：侦察兵发现了非零数字 1！

把 nums[fast] 和 nums[slow] 交换（也就是 1 和 0 交换）。

数组变成 [1, 0, 0, 3, 12]。

慢指针往前走：slow++（变成了 1）。

fast = 2（指向 0）：侦察兵看到 0，无视，fast++。

fast = 3（指向 3）：侦察兵发现了非零数字 3！

交换 nums[fast] 和 nums[slow]（也就是 3 和下标 1 的那个 0 交换）。

数组变成 [1, 3, 0, 0, 12]。

慢指针往前走：slow++（变成了 2）。

fast = 4（指向 12）：非零数字 12！

交换 nums[fast] 和 nums[slow]。

数组变成 [1, 3, 12, 0, 0]。

遍历结束。你看，所有的非零数字不仅按照原来的顺序排好了，而且那些被换过去的 0，极其自然地就被“挤”到了数组的最末尾！

3. C++ 满分优雅代码
利用 C++ 内置的 swap 函数，这段代码只有寥寥几行，但极其优雅：

C++
#include <vector>
#include <algorithm>

using namespace std;

class Solution {
public:
    void moveZeroes(vector<int>& nums) {
        int slow = 0; // 慢指针：指向下一个非零元素该放的位置

        // 快指针 i 遍历整个数组
        for (int fast = 0; fast < nums.size(); fast++) {
            // 只要遇到非零元素，就把他和慢指针的位置交换
            if (nums[fast] != 0) {
                swap(nums[slow], nums[fast]);
                slow++; // 慢指针往前挪一个坑位
            }
        }
    }
};
4. 复杂度分析
时间复杂度：O(N)。快指针只把整个数组从头到尾扫了一遍。

空间复杂度：O(1)。只用了 slow 和 fast 两个变量，全程原地交换，没有开辟任何额外空间。
*/

//我的代码：
class Solution {
public:
    void moveZeroes(vector<int>& nums) {
        int slow=0;//表示满指针
        for (int& num : nums)
        {
            if (num != 0)
            {
                swap(num, nums[slow]);
                slow++;
            }
        }
    }
};
