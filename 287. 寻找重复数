/*
这道题（LeetCode 287. 寻找重复数）绝对是面试官最喜欢用来“折磨”人的终极神题之一。

你仔细看看它的限制条件，简直苛刻到了变态的地步：

不能修改数组（把“排序法”和“原地交换法”直接枪毙）。

只能用 O(1) 的空间（把“哈希表”直接枪毙）。

时间复杂度还得过得去（把两层 for 循环暴力破解枪毙）。

被逼到绝境之后，我们只能请出这道题的“真神级”解法：把数组当成链表，用快慢指针找环！（也被称为 Floyd 判圈算法 / 龟兔赛跑算法）。

1. 核心思维：把数组变成“带环链表”
这道题有一个极其特殊的条件：数组里有 n+1 个数字，且所有的数字都在 1 到 n 之间。
这意味着什么？这就意味着，我们完全可以把数组里的“值”，当成下一个元素的“下标（指针）”！

我们拿一个数组来推演一下：nums = [1, 3, 4, 2, 2] （下标是 0 到 4，值是 1 到 4）。
我们从下标 0 开始，把 nums[i] 当作下一步要跳去的下标：

下标 0 里的值是 1 → 跳到下标 1

下标 1 里的值是 3 → 跳到下标 3

下标 3 里的值是 2 → 跳到下标 2

下标 2 里的值是 4 → 跳到下标 4

下标 4 里的值是 2 → 又跳回下标 2 了！

看懂了吗？因为有两个位置的值都是 2，所以它们最终都会指向下标 2。这就形成了一个死循环（环）！
而这个“环的入口”下标 2，恰恰就是那个重复的数字！

问题瞬间被降维打击：这根本不是一道数组题，这是一道求“带环链表入口节点”的题！

2. 神奇的“龟兔赛跑”（Floyd 判圈算法）
要在 O(1) 空间内找环的入口，这是快慢指针的拿手好戏。分为极其经典的两个阶段：

第一阶段：相遇（证明有环）

让“慢指针（乌龟）”每次走一步：slow = nums[slow]

让“快指针（兔子）”每次走两步：fast = nums[nums[fast]]

因为有环，兔子一定会从后面追上乌龟。当 slow == fast 时，它们在环内的某个点相遇了。

第二阶段：找入口（极其精妙的数学推导结论）

它们相遇后，我们把其中一个指针重新拉回起点（下标 0）。

然后，让这两个指针以相同的速度（每次一步）一起往前走。

当它们再次相遇的地方，绝对就是环的入口！（也就是那个重复的数字）。

(别问为什么第二阶段再相遇就是入口，这是极其严密的数学公式推导出来的死规律，记住这个结论就能通杀所有找环题！)

3. C++ 满分神仙代码
用快慢指针写出来的代码，完全不需要修改数组，也不需要额外空间，短小精悍得让人头皮发麻：

C++
#include <vector>

using namespace std;

class Solution {
public:
    int findDuplicate(vector<int>& nums) {
        // 起点都在下标 0
        int slow = 0;
        int fast = 0;

        // 阶段一：快慢指针出发，直到它们在环内相遇
        while (true) {
            slow = nums[slow];           // 乌龟走一步
            fast = nums[nums[fast]];     // 兔子走两步
            
            if (slow == fast) {
                break; // 相遇了，退出循环
            }
        }

        // 阶段二：把其中一个指针（比如 fast）拉回起点
        fast = 0;

        // 两个指针以相同的速度前进，再次相遇的地方就是环的入口（重复数字）
        while (slow != fast) {
            slow = nums[slow];
            fast = nums[fast];
        }

        return slow; // 返回 slow 或者 fast 都可以，它们现在指向同一个数字
    }
};
4. 复杂度分析
时间复杂度：O(N)。寻找相遇点和寻找入口点，指针走过的距离都是线性的，时间极快。

空间复杂度：绝对的 O(1)。全程只用了 slow 和 fast 两个整型变量，完美符合题目苛刻的要求。

顿悟时刻
把“值”当“指针”去用，是算法界极其高级的思维跳跃。这道题表面上考数组，背地里考的却是链表里最难的找环逻辑。

*/

//我的代码：
class Solution {
public:
    int findDuplicate(vector<int>& nums) {
        int fast = 0;
        int slow = 0;
        while (true)
        {
            slow = nums[slow];
            fast = nums[nums[fast]];

            if (slow == fast)  break;
        }
        fast = 0;
        while (fast!=slow)
        {
            slow = nums[slow];
            fast = nums[fast];
        }
        return fast;
    }
};
