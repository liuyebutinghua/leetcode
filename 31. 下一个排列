/*
这道题（LeetCode 31. 下一个排列）是一道极其神奇的“纯逻辑推导题”。它不考你什么高深的数据结构，纯粹考你能不能敏锐地察觉到数字排列背后的数学规律。

在面试中，只要你掌握了这个规律，代码写起来非常简单；但如果不知道规律，在考场上硬想，几乎是不可能做出来的。

我们先忘掉代码，直接用人类的直觉来看看“字典序”到底是怎么变大的。

1. 核心思维：如何让一个数字“变大一点点”？
假设我们有一个排列：[1, 3, 5, 4, 2]，你可以把它看成一个五位数 13542。
我们要找它的“下一个排列”，其实就是用这五个数字，拼出一个比 13542 刚好大一点点的数字。

怎么拼才能只大“一点点”？

尽量动后面的数字：因为前面的数字权重太大（比如万位、千位）。动后面的数字，增加的幅度才最小。

把后面的某个“大数”和前面的某个“小数”交换。

2. 手工推演：[1, 3, 5, 4, 2] 的完美转身
我们要按照以下四个极其严谨的步骤来做：

第一步：从右往左找“打破递增规律”的第一个小数（寻找替换位）

从右往左看，2 -> 4 -> 5，这三个数字是严格升序的（这意味着 542 已经是这三个数字能拼出的最大排列了，没法更大了）。

继续往左走，突然遇到了 3。3 比 5 小！规律被打破了！

这个 3 就是我们要替换掉的“小数”。（记下它的位置下标 i）

第二步：从右往左找第一个比 3 大的数（寻找替补）

我们要把 3 换掉，换成谁呢？必须换成右边比 3 大的数字里，最小的那一个，这样增加的幅度才最小。

因为右边的 [5, 4, 2] 已经是降序了，所以我们只要再从右往左扫描一遍，遇到的第一个比 3 大的数字，绝对就是最合适的替补！

从右往左看，遇到 2（比3小，不行），再遇到 4。4 比 3 大，就是你了！（记下它的下标 j）

第三步：互换小数和替补

把 3 和 4 交换。

数组变成了 [1, 4, 5, 3, 2]。

此时，千位从 3 变成了 4，数字确实变大了！

第四步：将后面的尾巴“翻转”成最小排列（擦屁股）

我们把千位变大了，为了让整个数字“只大一点点”，我们要把百位、十位、个位变得尽可能小。

注意看，交换后的尾巴 [5, 3, 2] 依然是降序的（最大的排列）。

怎么把它变成最小的排列？直接把它整个翻转过来！ 变成升序的 [2, 3, 5]。

最终结果：[1, 4, 2, 3, 5]。完美！

特殊情况：如果第一步从右往左找，发现整个数组全是升序的（比如 [5, 4, 3, 2, 1]），说明这就是全宇宙最大的排列了，不存在下一个更大的。题目要求直接重排成最小的，也就是直接执行第四步，把整个数组翻转变成 [1, 2, 3, 4, 5] 即可。

3. C++ 满分标准代码
C++ 的 STL 库里其实内置了一个函数叫 std::next_permutation，可以直接秒杀这道题。但面试官考你这道题，显然是想看你自己实现底层的这四个步骤。

这段代码极其干净利落：

C++
#include <vector>
#include <algorithm>

using namespace std;

class Solution {
public:
    void nextPermutation(vector<int>& nums) {
        int n = nums.size();
        if (n <= 1) return;

        int i = n - 2;
        // 第一步：从右往左，寻找第一个“左边小于右边”的数字 nums[i]
        while (i >= 0 && nums[i] >= nums[i + 1]) {
            i--;
        }

        // 如果 i >= 0，说明找到了这么一个需要被替换的小数
        if (i >= 0) {
            int j = n - 1;
            // 第二步：再从右往左，寻找第一个大于 nums[i] 的替补数字 nums[j]
            // 注意：因为右边已经是降序了，所以找到的第一个大于 nums[i] 的数，就是刚刚好大于它的最小数
            while (j >= 0 && nums[j] <= nums[i]) {
                j--;
            }
            // 第三步：交换它俩
            swap(nums[i], nums[j]);
        }

        // 第四步：无论有没有找到 i（即使整个数组是降序的），
        // 都需要把 i 之后的所有元素翻转，让它从降序变成升序，达到最小状态
        // reverse 是左闭右开区间，所以是从 nums.begin() + i + 1 一直到 nums.end()
        reverse(nums.begin() + i + 1, nums.end());
    }
};
4. 复杂度分析
时间复杂度：O(N)。最坏的情况下，找 i 遍历一遍，找 j 遍历一遍，最后翻转再遍历一遍。总共也就跑了大概三趟数组，属于标准的线性时间。

空间复杂度：O(1)。我们仅仅用了 i 和 j 两个辅助指针，利用 swap 和 reverse 在原数组上直接操作，完全符合题目要求。

这道题的精髓就在于你能不能画出那个波浪图，找到那个“下跌的转折点”。

*/
//我的代码：
class Solution {
public:
    void nextPermutation(vector<int>& nums) {
        int n = nums.size();

        if (n == 1) return;

        int i = n - 2;

        while (i >= 0 && nums[i] >= nums[i + 1])
        {
            i--;
        }

        if (i >= 0)
        {
            int j = n - 1;
            while (j >= 0 && nums[j] <= nums[i])
            {
                j--;
            }

            swap(nums[i],nums[j]);

        }

        //不管换没换 ，都需要翻转

        reverse(nums.begin() + i + 1,nums.end());

    }
};
