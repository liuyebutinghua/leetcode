/*
这道题（LeetCode 41. 缺失的第一个正数）在面试场上简直是“收割机”。
题目要求极其苛刻：时间复杂度 O(N)，且空间复杂度 O(1)。

不能排序（排序要 O(NlogN)）。

不能开辟新的哈希表（哈希表要 O(N) 的空间）。

被逼上绝路后，我们只能请出数组操作领域里最狂野的黑魔法：“原地哈希”（In-place Hashing），也就是传说中的“一个萝卜一个坑”！

1. 核心思维：让数组自己当自己的哈希表
仔细想一下，一个长度为 n 的数组，如果里面全是紧凑的正整数，那它最完美的状态应该是什么样？
肯定是 [1, 2, 3, ..., n] 这样排列的！

也就是说，数字 1 应该待在下标 0 的坑里，数字 2 应该待在下标 1 的坑里…… 数字 x 应该待在下标 x - 1 的坑里。

破局战术：
我们直接去遍历数组。当我们遇到一个数字 nums[i] 的时候：

它是负数、0，或者比数组长度 n 还要大？ 直接无视它！把它当成占着茅坑不拉屎的“垃圾”。因为我们最多只有 n 个坑，缺失的最小正整数绝对不可能超过 n+1。

它是一个合法的正整数（在 1 到 n 之间）？ 那我们就把它一脚踢到它真正应该待的坑位去！也就是把它和下标为 nums[i] - 1 的那个数字交换（swap）。

等我们把所有合法的数字都踢到属于它们自己的坑里之后，我们再从头巡视一遍坑位。
第一个发现“坑里的萝卜不对劲”的地方，那个坑对应的正确数字，就是缺失的最小正整数！

2. 手工推演：nums = [3, 4, -1, 1]
长度 n=4。合法的数字范围是 1 到 4。

看第 0 个坑 nums[0] = 3：

3 应该待在下标 2 的位置。把它和下标 2 的数字（-1）交换。

数组变成 [-1, 4, 3, 1]。

⚠️ 注意：换回来的新数字是 -1。它是垃圾，不用管了，往后走。

看第 1 个坑 nums[1] = 4：

4 应该待在下标 3 的位置。把它和下标 3 的数字（1）交换。

数组变成 [-1, 1, 3, 4]。

⚠️ 致命细节（为什么要用 while）： 换回来的新数字是 1！1 是合法数字，它应该待在下标 0。所以我们不能往前走，必须继续把它踢到它该去的地方！

把下标 1 的 1，和下标 0 的 -1 交换。

数组变成 [1, -1, 3, 4]。此时坑里是 -1（垃圾），终于可以往后走了。

看第 2 个坑 nums[2] = 3：

已经在正确的位置了，跳过。

看第 3 个坑 nums[3] = 4：

已经在正确的位置了，跳过。

第一遍“归位”结束，数组变成了 [1, -1, 3, 4]。

第二遍巡视：

下标 0 是 1，正确。

下标 1 是 -1，不对劲！这里本该是 2 的！

破案了，直接返回 2！

3. C++ 满分神级代码
注意看里面那个极其精妙的 while 循环，这是这道题成败的关键：

C++
#include <vector>
#include <algorithm>

using namespace std;

class Solution {
public:
    int firstMissingPositive(vector<int>& nums) {
        int n = nums.size();

        // 第一步：原地哈希，让“萝卜”各就各位
        for (int i = 0; i < n; i++) {
            // 只要当前萝卜是合法的（在 1 到 n 之间），
            // 且它没有待在正确的坑里，我们就疯狂交换它！
            // 为什么用 while？因为换回来的新萝卜可能也不在正确的坑里，需要继续踢走。
            while (nums[i] > 0 && nums[i] <= n && nums[nums[i] - 1] != nums[i]) {
                // 把 nums[i] 踢到下标 nums[i] - 1 的坑里去
                swap(nums[i], nums[nums[i] - 1]);
            }
        }

        // 第二步：巡视坑位，找第一个不对劲的
        for (int i = 0; i < n; i++) {
            if (nums[i] != i + 1) {
                return i + 1; // 发现空缺！返回正确的正数
            }
        }

        // 第三步：极其罕见的情况，如果全对上了（比如 [1, 2, 3]），那就返回下一个正数
        return n + 1;
    }
};
4. 🚨 面试官的夺命追问：这真的是 O(N) 吗？
你写了一个 for 循环，里面套了一个 while 循环去疯狂 swap。面试官立刻就会眉头一皱：“嵌套循环，你这不是退化成 O(N 
2
 ) 了吗？”

这时候你要气定神闲、面带微笑地向他解释：
“绝对是 O(N)。因为我们要看整体的交换次数！”
虽然外面有 for，里面有 while，但每一次进入 while 循环执行 swap，绝对会有一个萝卜被永远地安置到了正确的坑位里！
一个萝卜一旦到了正确的坑位，以后就再也不会被移动了。既然数组里最多只有 n 个萝卜，那么整个算法运行下来，swap 操作最多最多只会执行 n 次。
平摊到每一步，时间复杂度依然是绝对的线性 O(N)！
*/
//我的代码：
class Solution {
public:
    int firstMissingPositive(vector<int>& nums) {//一个萝卜一个坑

        int n = nums.size();

        for (int i = 0; i < n; i++)
        {
            while (nums[i]<=n&&nums[i]>0&&nums[nums[i]-1]!=nums[i])//如果相同就不换了
            {
                swap(nums[nums[i] - 1], nums[i]);
            }
        }

        for (int i = 0; i < n; i++)
        {
            if (nums[i] != i + 1)
            {
                return i + 1;
            }
        }

        return n+1;//如果全部对上了，最小的正整数是n+1

    }
};


