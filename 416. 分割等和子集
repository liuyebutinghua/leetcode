/*
解题思路：
Gemini 说
恭喜你！终于来到了动态规划里最威名赫赫的领地——“背包问题（Knapsack Problem）”。

这道题（LeetCode 416. 分割等和子集）是所有“0-1 背包问题”的绝对标杆和祖师爷。

前面你做的“零钱兑换”（凑金额）也是背包问题，但那是“完全背包”（硬币可以无限次使用）。而这道题是“0-1 背包”：每个数字（物品）只能用一次，要么放进背包（1），要么不放（0）。

我们来看看如何一眼看穿它的真面目！

1. 核心思维：数学翻译（化身装包大师）
题目要求：把数组分成两个和相等的子集。
稍微动动数学脑筋：

先把整个数组的所有数字加起来，算出一个总和 (sum)。

如果总和是奇数，那绝对不可能平分，直接返回 false。

如果总和是偶数，那我们的目标就非常明确了：我们能不能在数组里挑出几个数字，让它们的和刚好等于 sum / 2？

问题完美转化：
你现在有一个容量为 target = sum / 2 的背包。
数组里每个数字 nums[i]，既是它的重量，也是它的价值。
请问：你能不能把这个背包正好塞满？

2. 状态转移方程（灵魂拷问）
我们定义一个一维布尔数组 dp。
dp[j] 表示：能不能从数组里挑出几个数字，让它们的和刚好等于 j？

面对当前遍历到的数字 num，对于背包里的每一个容量 j，我们只有两个选择：

不把 num 放进包里：那能不能凑出 j，完全取决于之前能不能凑出 j（即原本的 dp[j]）。

把 num 放进包里：那能不能凑出 j，就看我们在放 num 之前，能不能凑出 j - num（即 dp[j - num]）。

只要这两个选择里有一个是 true，那 dp[j] 就是 true！
状态转移方程：

dp[j]=dp[j]∨dp[j−num]
3. 🚨 0-1 背包的绝对铁律：倒序遍历！
前面你做“零钱兑换”时，内层循环是从前往后遍历的。但在 0-1 背包里，内层循环（遍历容量）必须从大到小“倒序遍历”！

为什么？
假设你要凑容量 5，当前数字是 num = 2。

如果你正序遍历：当你算 dp[2] 时，发现可以把 2 放进去，dp[2] = true。接着你算 dp[4] 时，它会去看 dp[4 - 2] 也就是 dp[2]，发现是 true，于是 dp[4] 也变成了 true。

灾难发生了：你实际上把同一个数字 2，用了两次（拼出了 4）！这违背了 0-1 背包“每个数字只能用一次”的规则。

如果你倒序遍历：先算大的容量，再算小的容量。算 dp[4] 时依赖的 dp[2]，绝对是上一轮留下的干净状态，当前这个数字还没被放进去过，完美保证了每个数字只用一次！

4. C++ 满分代码实现
结合上面的思路，代码其实非常短，逻辑极其优美：

C++
#include <vector>
#include <numeric>

using namespace std;

class Solution {
public:
    bool canPartition(vector<int>& nums) {
        int sum = 0;
        // 算出总和
        for (int num : nums) {
            sum += num;
        }

        // 如果总和是奇数，绝对分不开
        if (sum % 2 != 0) return false;

        // 我们的目标是装满容量为 sum / 2 的背包
        int target = sum / 2;

        // dp[j] 表示是否能凑出和为 j 的子集
        vector<bool> dp(target + 1, false);
        
        // 边界条件：凑出和为 0 永远是可能的（什么都不选）
        dp[0] = true;

        // 外层循环：遍历每一个数字（物品）
        for (int num : nums) {
            // 内层循环：遍历背包容量。⚠️ 必须倒序！
            // 只要遍历到 j >= num 即可，因为包的容量连当前物品都装不下的话，就不用看了
            for (int j = target; j >= num; j--) {
                // 核心状态转移：不装它 vs 装它
                dp[j] = dp[j] || dp[j - num];
            }
        }

        // 最终看看能不能正好凑出目标和
        return dp[target];
    }
};
5. 复杂度分析
时间复杂度：O(N×target)，其中 N 是数组的长度，target 是数组总和的一半。这是一个典型的两层 for 循环。

空间复杂度：O(target)。我们把原本需要二维数组的状态压缩成了一维的滚动数组，极其节省空间。
*/

//我的代码：
class Solution {
public:
    bool canPartition(vector<int>& nums) {
        int num_sum = 0;

        for (int num : nums)//计算总和
        {
            num_sum += num;
        }

        if (num_sum % 2 == 1)return false;//和为奇数，没有办法分成两半

        int sum_target = num_sum / 2;
        vector<bool> dp(sum_target +1,false);
        dp[0] = true;

        for (int num : nums)
        {
            for (int j = sum_target; j >= num; j--)
            {
                dp[j] = dp[j] || dp[j-num];
            }


        }
        return dp[sum_target];

    }
};
