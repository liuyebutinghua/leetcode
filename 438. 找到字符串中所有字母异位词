/*
这道题（LeetCode 438）是滑动窗口领域里最完美的敲门砖，因为它完美融合了“字母异位词（哈希计数）”和“连续子串（双指针同向移动）”。

1. 核心思维：固定大小的“放大镜”
题目要求找 p 的异位词。既然是异位词，说明我们在 s 里要找的子串，长度必须和 p 绝对一样长！

这就是大名鼎鼎的定长滑动窗口。想象你手里拿着一个和 p 一样长的框框（放大镜），扣在字符串 s 上，然后一格一格往右挪。

怎么高效地检查框框里的字母对不对？
当然不能每次挪动都重新数一遍！你想想，框框往右挪一格，实际上框里的内容只发生了极其微小的变化：

右边界（进）：吃进来一个新字母。

左边界（出）：吐出去一个老字母。
中间的大部分字母根本没动！

所以，我们只需要维护两个长度为 26 的“频次数组”：

标准模板 p_count：提前数好 p 里每个字母的频次。

窗口状态 window_count：记录当前滑动窗口里的字母频次。每次滑动时，新字母频次 +1，老字母频次 -1。

只要 window_count 和 p_count 一模一样，说明此时框住的就是一个异位词！

2. C++ 满分极客代码
这里有一个极其优雅的 C++ 特性：C++ 的 vector 是重载了 == 运算符的！ 这意味着你可以直接用 if (p_count == window_count) 来比较两个数组的内容是否完全一致，底层极度优化，代码清爽到极致。

C++
#include <vector>
#include <string>

using namespace std;

class Solution {
public:
    vector<int> findAnagrams(string s, string p) {
        vector<int> result;
        int s_len = s.length();
        int p_len = p.length();

        // 保底防线：如果 s 还没 p 长，绝对不可能有异位词
        if (s_len < p_len) return result;

        // 维护两个长度为 26 的频次数组
        vector<int> p_count(26, 0);
        vector<int> window_count(26, 0);

        // 1. 初始化：先数好 p 的频次，同时把 s 的第一个窗口（长度为 p_len）填满
        for (int i = 0; i < p_len; i++) {
            p_count[p[i] - 'a']++;
            window_count[s[i] - 'a']++;
        }

        // 检查这最初的第一个窗口是不是答案
        if (p_count == window_count) {
            result.push_back(0); // 第一个窗口的起始下标是 0
        }

        // 2. 窗口开始滑动！从下标 p_len 开始，每次往右挪一格
        for (int i = p_len; i < s_len; i++) {
            // 吃进右边的新字母：当前下标 i
            window_count[s[i] - 'a']++;
            
            // 吐出左边的老字母：当前下标减去窗口长度
            window_count[s[i - p_len] - 'a']--;

            // 对比指纹！如果一模一样，记录当前窗口的起始下标
            if (p_count == window_count) {
                // 当前窗口的结束下标是 i，所以起始下标是 i - p_len + 1
                result.push_back(i - p_len + 1);
            }
        }

        return result;
    }
};
3. 复杂度分析发生了怎样的质变？
时间复杂度：O(N)。（N 是字符串 s 的长度）。我们只把字符串扫了一遍。每次滑动时，对比两个长度为 26 的 vector 需要 O(26)，在算法里这被视为常数时间 O(1)。所以整体速度快如闪电。

空间复杂度：O(1)。只开了两个长度固定为 26 的整型数组，以及一个存结果的数组。内存开销近乎为零。

顿悟时刻
定长滑动窗口的灵魂就是：有进必有出，维护增量状态。 绝不去重复计算中间那些没变动的数据。

这道题的窗口大小是“焊死”的，相对简单。但真实面试中最可怕、也是最能拉开水平差距的，是“动态滑动窗口”——也就是窗口的左右边界像毛毛虫一样，一会儿拉长，一会儿缩短。

*/

//我的代码：
class Solution {
public:
    vector<int> findAnagrams(string s, string p) {
        vector<int> result;
        int s_len = s.size();
        int p_len = p.size();

        if (p_len > s_len) return result;

        vector<int> p_num(26, 0);
        vector<int> s_num(26, 0);
        
        for (int i = 0; i < p_len; i++)
        {
            p_num[p[i] - 'a']++;
            s_num[s[i] - 'a']++;
        }

        if (p_num == s_num)
        {
            result.push_back(0);
        }

        for (int i = p_len; i < s_len; i++)
        {
            s_num[s[i] - 'a']++;
            s_num[s[i - p_len] - 'a']--;

            if (s_num == p_num)
            {
                result.push_back(i-p_len+1);
            }

        }

        return result;
    }
};



