/*
解题思路：
贪心算法：
1. 核心思维：划分“势力范围”（步步为营）
想象你是一个将军在带兵打仗，你的策略不是看眼前这一步该跳多远，而是按“批次（范围）”来推进。

我们维护三个变量：

max_reach：我们在探索过程中，发现的未来的最远可达位置（和上一题一模一样）。

current_end：当前这一跳的“势力范围”边界。

jumps：记录我们跳跃的次数。

游戏规则极其简单：
我们从左到右遍历数组。每走到一个格子，我们就用它刷新未来的 max_reach。
关键点来了：当我们一直往前走，走到当前这一跳的边界 current_end 时，意味着什么？
意味着：不管我之前在这个范围内怎么磨蹭，现在我所在的这片区域已经到头了，我被迫必须再跳一次了！

被迫跳跃时：

jumps 次数加 1。

既然跳了，我的新边界 current_end 就变成了刚才一路走来发现的那个最远的 max_reach。

2. 手工推演：nums = [2, 3, 1, 1, 4]
初始状态：jumps = 0, current_end = 0, max_reach = 0。

站在第 0 天（值 2）：

能摸到的最远距离 max_reach = 0 + 2 = 2。

哎呀，走到当前的边界 current_end (0) 了！必须跳 1 次！

跳跃次数 jumps 变成 1。

更新新边界 current_end 为 2。

（潜台词：第 1 跳的势力范围是下标 1 和 2）

走到第 1 天（值 3）：

能摸到的最远距离 max_reach = max(2, 1 + 3) = 4。（发现新大陆了！）

还没到边界（当前边界是 2），继续往前探。

走到第 2 天（值 1）：

能摸到的最远距离 max_reach = max(4, 2 + 1) = 4。

哎呀，走到当前的边界 current_end (2) 了！必须再跳 1 次！

跳跃次数 jumps 变成 2。

更新新边界 current_end 为 4。

（潜台词：第 2 跳的势力范围直接覆盖到了最后）

此时新边界是 4，已经摸到终点了，游戏结束。总共最少跳 2 次！

4. 为什么这段代码能稳拿最小跳跃次数？这就是贪心的精髓所在：

在当前的势力范围 [左边界, 右边界] 内，我们绝不提前跳。

我们一定要把这片区域里的所有格子都“压榨”一遍，看看谁能提供最远的 max_reach。

只有当迫不得已（走到了右边界 current_end）时，我们才心甘情愿地消耗 1 次跳跃机会，

并直接跳到那个能带我们走得最远的地方去。这种“延迟决策”保证了每一次跳跃的价值最大化。

时间复杂度：$O(N)$，只需要遍历一次数组。空间复杂度：$O(1)$，只用了三个整型变量。

结合刚才第一题的 max_reach，再看这道题用 current_end 强制触发跳跃的逻辑，

是不是觉得这种贪心的思路非常有快感？

*/

//AI的代码：
class Solution {
public:
    int jump(vector<int>& nums) {
        int n = nums.size();
        // 如果数组只有一个元素，不用跳就已经在终点了
        if (n == 1) return 0;

        int jumps = 0;       // 记录跳跃次数
        int current_end = 0; // 当前这一跳的势力范围边界
        int max_reach = 0;   // 探索过程中发现的最远可达位置

        // 注意：遍历到 n - 2 就行了，因为不需要从最后一个格子再起跳
        for (int i = 0; i < n - 1; i++) {
            // 走到每个格子，都尝试刷新最远可达位置
            max_reach = max(max_reach, i + nums[i]);

            // 如果走到了当前这一跳的边界，就被迫必须跳跃了
            if (i == current_end) {
                jumps++;                 // 跳跃次数 +1
                current_end = max_reach; // 势力范围更新为发现的最远位置
                
                // 小优化：如果更新后的势力范围已经覆盖了终点，直接提前下班
                if (current_end >= n - 1) {
                    break;
                }
            }
        }

        return jumps;
    }
};

//我的代码：
class Solution {
public:
    int jump(vector<int>& nums) {
        int n = nums.size();

        if (n == 1) return 0;


        int current_end = 0;
        int max_range = 0;
        int jump_nums = 0;
        for (int i = 0; i < n - 1; i++)
        {
            max_range = max(max_range, i+nums[i]);

            if (i == current_end)
            {
                current_end = max_range;
                jump_nums++;
                if (max_range >= n - 1)
                {
                    break;
                }
            }
        }

        return jump_nums;

    }
};
