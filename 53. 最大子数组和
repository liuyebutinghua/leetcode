/*
这道题（LeetCode 53. 最大子数组和）在算法界的地位，基本上就相当于武侠小说里的“降龙十八掌”第一式。它不仅是无数人面试遇到的第一道必考题，更是大名鼎鼎的卡丹算法（Kadane's Algorithm）的诞生地。

很多新手一上来会想着用两层 for 循环去枚举所有的子数组起点和终点，时间复杂度直接飙到 O(N 
2
 )，在面对长数组时绝对会超时。

其实，只要转换一下思维，用“及时止损”的人生哲学来看待这道题，你就能在绝对的 O(N) 时间和 O(1) 空间内秒杀它！

1. 核心思维：抛弃“负资产”，及时止损
想象你正在玩一个沿途捡金币（正数）和踩扣钱陷阱（负数）的游戏。你必须走连续的一段路，目标是让手里的总资产达到最大。

当你走到第 i 个数字时，你面临一个灵魂拷问：我是应该把自己加到前面的队伍里，还是干脆自己另起炉灶、从我这里重新算作一个新队伍的开头？

判断的标准极其简单粗暴：
看看前面的队伍带给你的到底是“收益”还是“负债”！

如果前面累加的总和是负数：不管你当前是正还是负，前面的负数总和只会拖累你！果断抛弃前面的所有历史，“及时止损”，把当前数字作为新子数组的起点。

如果前面累加的总和是正数：不管你当前是正是负，前面的正数总和都能给你带来增益（哪怕你当前是负的，前面攒下的家底也能帮你扛一扛）。那就毫不犹豫地加进去！

用动态规划的状态转移方程来表达，就是极其优美的：

dp[i]=max(nums[i],dp[i−1]+nums[i])
2. 手工推演：nums = [-2, 1, -3, 4, -1, 2, 1]
我们只需要两个变量：current_sum（记录当前连着走的队伍总和）和 max_sum（记录历史最高纪录）。

遇到 -2：current_sum = -2，最高纪录 max_sum = -2。

遇到 1：前面的总和是 -2（负资产，拖后腿）。果断扔掉前面，从自己重新开始！current_sum = 1。最高纪录更新为 1。

遇到 -3：前面的总和是 1（正资产，能帮一把）。加进去！current_sum = 1 + (-3) = -2。最高纪录还是 1。

遇到 4：前面的总和是 -2（负资产）。果断扔掉，从自己开始！current_sum = 4。最高纪录更新为 4。

遇到 -1：前面的总和是 4。加进去！current_sum = 3。最高纪录还是 4。

遇到 2：前面的总和是 3。加进去！current_sum = 5。最高纪录更新为 5。

遇到 1：前面的总和是 5。加进去！current_sum = 6。最高纪录更新为 6。

最终答案就是 6（也就是子数组 [4, -1, 2, 1]）。

3. C++ 满分极客代码
把上面的逻辑翻译成代码，你连 O(N) 的 dp 数组都不需要开，直接用一个变量滚动刷新，把空间复杂度压榨到绝对的 O(1)：

C++
#include <vector>
#include <algorithm>

using namespace std;

class Solution {
public:
    int maxSubArray(vector<int>& nums) {
        // 初始化：先用数组的第一个元素打底
        int current_sum = nums[0];
        int max_sum = nums[0];

        // 从第二个元素开始遍历
        for (int i = 1; i < nums.size(); i++) {
            // 核心哲学：如果前面的累计是负资产，直接一脚踢开，从自己重新开始
            // 否则，就把自己加到前面的队伍里
            if (current_sum < 0) {
                current_sum = nums[i];
            } else {
                current_sum += nums[i];
            }

            // 每次走完一步，都看看有没有打破历史最高纪录
            max_sum = max(max_sum, current_sum);
        }

        return max_sum;
    }
};
(注：这里 if-else 的逻辑其实等价于 current_sum = max(nums[i], current_sum + nums[i])，写成 if-else 会让面试官更容易看懂你的“止损”思维。)

4. 复杂度分析
时间复杂度：O(N)。从头到尾只把数组扫了一遍。

空间复杂度：O(1)。只用了两个整型变量，极其轻量。
*/

//我的代码1：
class Solution {
public:
    int maxSubArray(vector<int>& nums) {
        int sum = 0;
        int max_sum = INT_MIN;

        for (int& num : nums)
        {

            if(sum<0)//前面的和对我来说是负值，抛弃
            {
                sum = num;
            }
            else//前面的和对我来说是非负值，是有效的，可以留着
            {
                sum += num;
            }
            max_sum = max(max_sum,sum);
        }
        return max_sum;
    }
};

//我的代码2：
int maxSubArray(int* nums, int numsSize) {
    // 初始化当前子数组和与全局最大和
    int current_sum = nums[0];
    int max_sum = nums[0];

    // 遍历数组
    for (int i = 1; i < numsSize; i++) {
        // 更新当前子数组和
        if (current_sum < 0) {
            current_sum = nums[i]; // 如果当前子数组和小于 0，则重置为当前元素
        }
        else {
            current_sum += nums[i]; // 否则加上当前元素
        }

        // 更新全局最大和
        if (current_sum > max_sum) {
            max_sum = current_sum;
        }
    }

    return max_sum;
}


