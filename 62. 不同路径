/*
如果说“爬楼梯”是一维 DP 的入门，“打家劫舍”是带选择的一维 DP，那么这道“不同路径”（LeetCode 62）就是二维 DP 的绝对经典入门题！

你可以把它直接理解为：二维版的“爬楼梯”。

1. 核心思维：看头顶和看左边
在“爬楼梯”里，你只能从 n−1 阶或者 n−2 阶爬上来，所以当前的方法数等于前两阶之和。

在这道题里，机器人只能向下或向右移动。
假设我们要走到坐标为 (i, j) 的格子上，请问机器人最后一步是怎么跨进来的？
显然只有两种可能：

从它正上方的格子 (i-1, j) 向下一步走过来。

从它正左方的格子 (i, j-1) 向右一步走过来。

灵魂状态转移方程呼之欲出：
走到当前格子的总路线数，等于走到它上方的路线数，加上走到它左边的路线数！

dp[i][j]=dp[i−1][j]+dp[i][j−1]
2. 找准地基（边界条件）
既然公式是要回头看“上面”和“左边”，那如果格子根本没有上面，或者没有左边怎么办？这就是我们的边界。

最上面的一排（第 0 行）：机器人只能一直向右走才能到达这些格子，没有任何其他岔路。所以这一排所有的格子，到达它们的方法只有 1 种。

最左边的一列（第 0 列）：同理，机器人只能一直向下走。到达这一列所有格子的方法也只有 1 种。

这两条边，就是我们盖起这座高楼的地基！

3. C++ 满分代码实现（标准二维 DP）
顺着这个思路，我们只需要建一个 m 行 n 列的二维数组，先把两边的“护城河”填满 1，然后用两个 for 循环把中间的格子填满就行了。

C++
#include <vector>

using namespace std;

class Solution {
public:
    int uniquePaths(int m, int n) {
        // 创建一个 m 行 n 列的二维 dp 数组，初始值全设为 0
        // dp[i][j] 表示走到坐标 (i, j) 的路径总数
        vector<vector<int>> dp(m, vector<int>(n, 0));

        // 1. 打地基：最左边一列全是 1（因为只能一直往下走）
        for (int i = 0; i < m; i++) {
            dp[i][0] = 1;
        }
        
        // 1. 打地基：最上面一行全是 1（因为只能一直往右走）
        for (int j = 0; j < n; j++) {
            dp[0][j] = 1;
        }

        // 2. 开始推演：从坐标 (1, 1) 开始填表
        for (int i = 1; i < m; i++) {
            for (int j = 1; j < n; j++) {
                // 核心状态转移方程：当前路径数 = 顶上的路径数 + 左边的路径数
                dp[i][j] = dp[i - 1][j] + dp[i][j - 1];
            }
        }

        // 3. 右下角的终点，就是我们要的最终答案
        return dp[m - 1][n - 1];
    }
};
4. 复杂度分析
时间复杂度：O(m×n)。因为我们需要把整个网格里的每一个格子都算一遍。

空间复杂度：O(m×n)。我们开辟了一个同样大小的二维数组 dp。

*/



//我的代码：
class Solution {
public:
    int uniquePaths(int m, int n) {
        vector<vector<int>> dp(m, vector<int>(n, 0));

        for (int i = 0; i < m; i++)
        {
            dp[i][0] = 1;
        }

        for (int j = 0; j < n; j++)
        {
            dp[0][j] = 1;
        }

        for (int i = 1; i < m; i++)
        {
            for (int j = 1; j < n; j++)
            {
                dp[i][j] = dp[i - 1][j] + dp[i][j - 1];
            }
        }

        return dp[m - 1][n - 1];
    }
};


/*
空间压缩（代码极客）：你发现了吗？在算当前这一行的时候，我们其实只用到了当前行（左边的元素）和上一行（上方的元素），再往上的历史数据完全没用了。
你能把空间复杂度从二维的 $O(m \times n)$ 压缩到一维的 $O(n)$ 吗？

我们来看看，这招“降维打击”到底是怎么把一个庞大的二维网格，生生压扁成一根线的！

1. 核心思维：历史数据真的有用吗？
在原本的二维数组里，我们用的状态转移方程是：
dp[i][j] = dp[i-1][j] + dp[i][j-1]
（当前格子 = 正上方的格子 + 正左方的格子）

你仔细盯着我们在填表时的动作：
当我们在填第 2 行的数据时，我们真的需要第 0 行的数据吗？
完全不需要！我们只需要紧挨着它的第 1 行的数据。
一旦第 2 行填完了，第 1 行的数据也就彻底失去利用价值了。

这就好比我们在玩“俄罗斯方块”，只有最上面那一层是活跃的，底下已经被压实的行我们根本不关心。

既然如此，我们为什么要开辟一个 m * n 的庞大矩阵把所有历史记录都存下来呢？
我们只需要一个长度为 n 的一维数组（相当于一根可以滚动的进度条），让它一层一层地往下刷新就行了！

2. 神奇的“原地覆盖”（1D 状态转移）
假设我们只用一个一维数组 dp[j]。
当我们准备更新 dp[j] 的时候：

等号右边的 dp[j]：由于它还没被当前这一行的新数据覆盖，所以它里面存的，刚好就是上一行这个位置的数据（也就是原二维数组里的 dp[i-1][j]，正上方的值）。

等号右边的 dp[j-1]：因为我们是从左往右填表的，所以它刚刚被当前这一行更新过。它里面存的，刚好就是当前行前一个位置的数据（也就是原二维数组里的 dp[i][j-1]，正左方的值）。

于是，那个二维的公式，极其奇妙地坍缩成了一维：
dp[j] = dp[j] + dp[j - 1]
（新自己 = 老自己 + 左边的邻居）

3. 手工推演：3 行 3 列的网格
我们申请一个长度为 3 的一维数组 dp。
初始状态（打地基）：第 0 行全是 1。所以我们直接把一维数组初始化为：
dp = [1, 1, 1]

开始遍历第 1 行：

算 dp[1]：dp[1] = dp[1] (头顶的1) + dp[0] (左边的1) = 2。此时 dp = [1, 2, 1]。

算 dp[2]：dp[2] = dp[2] (头顶的1) + dp[1] (左边刚算出的2) = 3。此时 dp = [1, 2, 3]。
(看！这不就是二维网格里第 1 行的真实数据吗！)

开始遍历第 2 行（只用这同一根进度条覆盖）：

算 dp[1]：dp[1] = dp[1] (头顶的2) + dp[0] (左边的1) = 3。此时 dp = [1, 3, 3]。

算 dp[2]：dp[2] = dp[2] (头顶的3) + dp[1] (左边刚算出的3) = 6。此时 dp = [1, 3, 6]。

遍历结束！答案就是数组的最后一个数字 6。

4. C++ 满分极简代码
理解了逻辑之后，代码短得简直像是在作弊：

C++
#include <vector>

using namespace std;

class Solution {
public:
    int uniquePaths(int m, int n) {
        // 1. 申请一个长度为 n 的一维数组，并将初始值（第0行）全部填满为 1
        vector<int> dp(n, 1);

        // 2. 从第 1 行开始往下滚动刷新（外层控制行，内层控制列）
        for (int i = 1; i < m; i++) {
            for (int j = 1; j < n; j++) {
                // 核心状态压缩公式：新值 = 头顶的旧值 + 左边刚算出的新值
                dp[j] = dp[j] + dp[j - 1]; 
            }
        }

        // 3. 进度条滚到最后一行时，最右边的值就是终点答案
        return dp[n - 1];
    }
};
5. 复杂度发生了怎样的质变？
时间复杂度：依然是 O(m×n)。因为每个格子我们还是要算一次加法。

空间复杂度：从 O(m×n) 暴降到了 O(n)！
*/
//我的代码
class Solution {
public:
    int uniquePaths(int m, int n) {
        vector<int> dp(n, 0);

        dp[0] = 1;

        for (int j = 0; j < m; j++)
        {
            for (int i = 1; i < n; i++)
            {
                dp[i] = dp[i] + dp[i - 1];
            }

        }

        return dp[n - 1];
};






