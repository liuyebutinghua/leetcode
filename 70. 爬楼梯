/*
解题思路：
1. 核心思维：时光倒流法（找递推关系）面对这道题，不要从第 1 阶往上顺着想，那会产生无数种分支，让大脑瞬间宕机。
我们换个视角，从终点往下看。假设你要爬到第 $n$ 阶楼梯。请问：你最后一步是怎么跨上第 $n$ 阶的？

因为题目规定每次只能爬 1 阶或 2 阶，所以无论你前面怎么瞎折腾，你最后跨上终点的那一下，只有两种可能：
从第 $n-1$ 阶跨了 1 步上来。从第 $n-2$ 阶跨了 2 步上来。这说明了什么？走到第 $n$ 阶的所有方法总数，
正好等于走到第 $n-1$ 阶的方法数，加上走到第 $n-2$ 阶的方法数！我们用数学公式表示，
这就是动态规划里最核心的状态转移方程：$$dp[n] = dp[n-1] + dp[n-2]$$(如果你学过数学，你会立刻发现：
这不就是大名鼎鼎的斐波那契数列吗！)

2. 手工推演与边界条件既然要算后面的，就得先知道前面的。最底层的地基（边界条件）是什么呢？
第 1 阶：只能跨 1 步。共 1 种方法。
第 2 阶：可以一次跨 2 步，也可以跨两次 1 步。共 2 种方法。
有了地基，我们就可以一层层往上盖楼了：
第 3 阶的方法数 = 第 2 阶 + 第 1 阶 = 2 + 1 = 3 种。
第 4 阶的方法数 = 第 3 阶 + 第 2 阶 = 3 + 2 = 5 种。
第 5 阶的方法数 = 第 4 阶 + 第 3 阶 = 5 + 3 = 8 种。
……以此类推。

3. 代码演进：从一维数组到“终极压缩”初级写法（用一维数组，空间复杂度 $O(n)$）：
你可以开一个大小为 $n+1$ 的数组 dp，然后写个 for 循环，把 dp[i] = dp[i-1] + dp[i-2] 填满。
这绝对能通过，但面试官会问：还能不能优化空间？终极满分写法（状态压缩，空间复杂度 $O(1)$）：
仔细观察，我们在计算第 $n$ 阶时，其实只用到了它前面的两个人（$n-1$ 和 $n-2$），再往前的数据完全没用了！
既然如此，我们干嘛要建一个长长的数组把历史全存下来呢？我们只需要用两个变量，交替滚动记录“前两个状态”就足够了！



*/



//ai的代码
class Solution {
public:
    int climbStairs(int n) {
        // 边界情况直接返回
        if (n == 1) return 1;
        if (n == 2) return 2;

        // 只需要两个变量来记录“前两个台阶”的方法数
        int prev2 = 1; // 相当于 dp[1]
        int prev1 = 2; // 相当于 dp[2]
        int current = 0; // 相当于 dp[i]

        // 从第 3 阶开始往上爬
        for (int i = 3; i <= n; i++) {
            // 当前台阶的方法数 = 前一个台阶 + 前两个台阶
            current = prev1 + prev2;
            
            // 爬完这阶后，所有人一起往前挪一步，为下一轮循环做准备
            prev2 = prev1;
            prev1 = current;
        }

        return current;
    }
};


//我的代码：
class Solution {
public:
    int climbStairs(int n) {
        if (n == 1) return 1;
        if (n == 2) return 2;


        int prev1 = 1;
        int prev2 = 2;
        int current = 0;

        for (int i = 3; i <= n; i++)
        {
            current = prev2 + prev1;
            prev1 = prev2;
            prev2 = current;
        }

        return current;

    }
};
