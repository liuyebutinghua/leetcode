/*
Gemini 说
太震撼了！你终于站在了滑动窗口（Sliding Window）领域的最终 Boss 面前！

这道题（LeetCode 76. 最小覆盖子串）是无数大厂面试的终极梦魇，名副其实的 Hard 题。之前我们做“找到所有字母异位词”时，窗口的大小是固定的（等于 p 的长度）。而在这道题里，窗口的大小是未知且动态伸缩的。

只要你彻底弄懂了这道题的“毛毛虫”爬行逻辑，以后 LeetCode 上所有的滑动窗口题，对你来说都只是默写模板而已！

1. 核心思维：动态伸缩的“毛毛虫”
要想在 O(M+N) 的时间里找到最短的子串，我们需要两个指针 left 和 right，把它们想象成一条毛毛虫的尾巴和头。

绝对无敌的“三步走”爬行法则：

猛吃（右指针扩大窗口）：right 疯狂往右走，吃进字符。只要当前窗口里的字符还凑不齐目标字符串 t，right 就绝不停下。

反刍（左指针缩小窗口）：当窗口终于把 t 里的所有字符都凑齐时（可能包含很多多余的废字符），我们开始贪心！让 left 往右走，把最左边的废字符吐出去。

记录与逼近：在 left 吐字符的过程中，只要窗口依然满足条件，我们就一直更新“最短长度”。直到 left 吐出了一个极其关键的字符（导致窗口不再凑齐 t），我们才停止收缩，重新让 right 去右边觅食。

2. 🚨 极其精妙的优化：如何判断“凑齐了”？
如果在每次滑动时，都去遍历对比两个哈希表看是不是凑齐了，那绝对会超时！
顶级黑客技巧：引入一个 valid（达标计数器）。

假设 t = "ABC"。我们不仅需要 A、B、C 各出现 1 次。

我们用一个 need 哈希表记录 t 的需求：{A:1, B:1, C:1}。

用一个 window 哈希表记录当前窗口的拥有量。

当 window 里的 A 数量达到了 need 里的要求时，我们的 valid 变量就加 1。

当 valid == need.size()（也就是 3 种字符全都达标了）时，就说明彻底凑齐了！极速 O(1) 判断！

3. C++ 满分工业级模板代码
这段代码的结构是滑动窗口的绝对圣经。只要背下这个框架，什么“最长无重复子串”、“最大连续1的个数”，全部可以套用：

C++
#include <string>
#include <unordered_map>
#include <climits>

using namespace std;

class Solution {
public:
    string minWindow(string s, string t) {
        // need 记录需要的字符和数量，window 记录窗口内拥有的字符和数量
        unordered_map<char, int> need, window;
        for (char c : t) need[c]++;

        int left = 0, right = 0;
        int valid = 0; // 记录窗口中已经“达标”的字符种类数

        // 记录最小覆盖子串的起始位置和长度
        int start = 0, min_len = INT_MAX;

        // 1. 毛毛虫的头开始向右探索
        while (right < s.size()) {
            char c = s[right]; // c 是将移入窗口的字符
            right++; // 右指针右移，扩大窗口
            
            // 如果这个字符是 t 里面需要的
            if (need.count(c)) {
                window[c]++;
                // 当窗口内该字符的数量刚好达到需要的数量时，达标种类数 +1
                if (window[c] == need[c]) {
                    valid++;
                }
            }

            // 2. 判断毛毛虫的尾巴是否需要收缩（只要所有的字符种类都达标了，就必须收缩）
            while (valid == need.size()) {
                // 🌟 在收缩之前，先看看是不是破了历史最短记录！
                if (right - left < min_len) {
                    start = left;
                    min_len = right - left;
                }
                
                char d = s[left]; // d 是将移出窗口的字符
                left++; // 左指针右移，缩小窗口
                
                // 如果移出去的字符刚好是 t 里面需要的
                if (need.count(d)) {
                    // 如果移出去之后，该字符的数量不再达标，达标种类数 -1
                    if (window[d] == need[d]) {
                        valid--;
                    }
                    window[d]--;
                }
            }
        }
        
        // 如果 min_len 还是初始值，说明根本没找到，返回空串；否则截取子串返回
        return min_len == INT_MAX ? "" : s.substr(start, min_len);
    }
};
4. 复杂度发生了怎样的质变？
时间复杂度：O(M+N)（M 是 s 的长度，N 是 t 的长度）。虽然有个嵌套的 while 循环，但你仔细看，left 和 right 两个指针都只会一直往右走，绝不回头。s 里的每个字符最多进窗口一次，出窗口一次。绝对的线性时间！

空间复杂度：O(C)（C 是字符集的大小）。在这个题里，字符最多只有大小写英文字母，所以哈希表的大小不会超过 128（ASCII 码的长度），空间被死死限制在极小的常数级别，等同于 O(1)！
*/

//我的代码：
class Solution {
public:
    string minWindow(string s, string t) {
        unordered_map<char, int> need, windows;
        int left = 0, right = 0, start = 0,valid=0;
        int min_len = INT_MAX;

        for (char& c : t)
        {
            need[c]++;
        }


        while (right < s.size())//注意  ：这个公式中的顺序不能变
        {
            char c = s[right];
            right++;

            if (need.count(c))//是t需要的
            {
                windows[c]++;
                if (windows[c] == need[c])
                {
                    valid++;
                }
            }

            while (valid == need.size())
            {

                if (min_len > right - left)//记录最新的短的
                {
                    min_len = right - left;
                    start = left;
                }

                char d = s[left];
                left++;

                if (need.count(d))//如果删除的是t需要的
                {
                    if (windows[d] == need[d])
                    {
                        valid--;
                    }
                    windows[d]--;
                }

            }

        }
        return min_len == INT_MAX ? "" : s.substr(start, min_len);

    }
};




