/*
解题思路：
贪心算法：
1. 核心思维：不断扩张的“势力范围”
题目要求“同一字母最多出现在一个片段中”。
这意味着什么？
假设当前片段里包含了一个字母 'a'，而整个字符串中最后一个 'a' 出现在下标 8 的位置。那么，为了把所有的 'a' 都圈进来，当前这个片段的右边界，最起码也要延伸到下标 8！

但在延伸到下标 8 的这半路上，我们可能还会遇到其他字母，比如 'b'。如果最后一个 'b' 在下标 12，那没办法，我们的“势力范围”被迫再次扩张，右边界必须更新到 12。

破局策略（两步走）：

第一遍（上帝视角）：先遍历一次字符串，查清楚每个字母在字符串里最后一次出现的位置。相当于我们提前拿到了剧本。

第二遍（步步为营）：再遍历一次字符串，每走到一个字母，就查剧本，看看它的最后出现位置，并用它来刷新当前片段的右边界 (end)。

什么时候这个片段算彻底结束了？

当当前走到的下标 i，正好等于我们一直在努力扩张的右边界 end 时！（这和跳跃游戏 II 里的 if (i == current_end) 简直一模一样！）

此时，一刀切下去，记录这个片段的长度，然后开启下一个新片段。

2. 手工推演：s = "ababcc"
第一遍（查剧本）：记录每个字母的最远下标

'a' 最远在下标 2。

'b' 最远在下标 3。

'c' 最远在下标 5。

第二遍（切割）：
初始状态：start = 0, end = 0。

走到 s[0] = 'a'：查剧本，'a' 最远到下标 2。当前边界更新 end = max(0, 2) = 2。

走到 s[1] = 'b'：查剧本，'b' 最远到下标 3。边界被迫扩张！end = max(2, 3) = 3。

走到 s[2] = 'a'：查剧本，'a' 最远到 2。边界 end 依然是 3。

走到 s[3] = 'b'：查剧本，'b' 最远到 3。边界 end 依然是 3。

关键时刻： 当前下标 i 走到 3 了，正好等于边界 end (3)！

说明在这个范围内出现的所有字母，在后面再也不会出现了。一刀切！

计算长度：end - start + 1 = 3 - 0 + 1 = 4。把 4 加入结果。

更新下一个片段的起点：start = i + 1 = 4。

走到 s[4] = 'c'：查剧本，'c' 最远到下标 5。更新边界 end = max(3, 5) = 5。

走到 s[5] = 'c'：查剧本，'c' 最远到下标 5。边界 end 依然是 5。

关键时刻： 下标 i (5) 等于边界 end (5)。一刀切！

计算长度：5 - 4 + 1 = 2。把 2 加入结果。

最后返回结果 [4, 2]。完美！


*/


//ai的代码
class Solution {
public:
    vector<int> partitionLabels(string s) {
        int hash[26] = {0}; // 充当剧本，记录每个字母最后出现的位置
        int n = s.size();
        
        // 1. 第一遍遍历：统计每一个字符最后出现的位置
        for (int i = 0; i < n; i++) {
            // s[i] - 'a' 可以把字母映射到 0~25 的索引上
            hash[s[i] - 'a'] = i; 
        }

        vector<int> result;
        int start = 0; // 当前片段的起始位置
        int end = 0;   // 当前片段的最远边界

        // 2. 第二遍遍历：找边界并切割
        for (int i = 0; i < n; i++) {
            // 走到一个字符，尝试用它的最后出现位置来扩张当前边界
            end = max(end, hash[s[i] - 'a']);

            // 如果当前走到的位置，已经达到了当前不断扩张的最远边界
            // 说明该切断了！
            if (i == end) {
                // 计算当前片段的长度
                result.push_back(end - start + 1);
                // 更新下一个片段的起点
                start = i + 1;
            }
        }

        return result;
    }
};


//我的代码
class Solution {
public:
    vector<int> partitionLabels(string s) {
        int n = s.size();
        int hash[26] = { 0 };

        for (int i = 0; i < n; i++)
        {
            hash[s[i] - 'a'] = i;
        }

        int start = 0;
        int current_end = 0;
        vector<int> result;

        for (int i = 0; i < n; i++)
        {
            current_end = max(current_end, hash[s[i] - 'a']);

            if (current_end == i)
            {
                result.push_back(current_end- start+1);
                start = i + 1;

            }


        }
        return result;


    }
};
