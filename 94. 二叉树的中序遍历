/*
这道题（LeetCode 94. 二叉树的中序遍历）是所有二叉树算法的“开山鼻祖”。搞懂了它，你就等于拿到了整片森林的地图。

1. 核心思维：什么是“中序”，什么是“递归魔法”？
在二叉树里，所谓的前序、中序、后序，全都是看“根节点（自己）”在什么时候出场。

前序：我先上（中 → 左 → 右）

中序：我等左边的小弟完事了再上（左 → 中 → 右）

后序：我等左右两边的小弟都完事了，我最后上（左 → 右 → 中）

中序遍历（Inorder Traversal）的物理直觉：
想象你在二叉树的正上方用手电筒往下照，所有的节点垂直掉落到地上排成一排。你会发现，它们落到地上的顺序，刚好就是从左到右的升序排列！（在“二叉搜索树”中，中序遍历出来的结果绝对是严格递增的序列）。

递归魔法的本质：
对于一棵参天大树，我们人类的大脑是无法一眼看穿怎么走的。这时候，我们只需要写出“作为一个普通节点的本分”，剩下的全部甩锅给系统！
作为一个中序遍历的节点，我的本分只有三件事，并且严格按顺序执行：

踹一脚我的左孩子：“喂，你去把你的左半边都遍历完！”

等左孩子干完活了，我把我自己（的值）记录下来。

踹一脚我的右孩子：“喂，你去把你的右半边都遍历完！”

2. C++ 满分极客代码（3 行代码的暴力美学）
在二叉树的递归题里，我们通常会写一个辅助函数 traversal。你看看这个逻辑，是不是简单得令人发指：

C++
/**
 * Definition for a binary tree node.
 * struct TreeNode {
 * int val;
 * TreeNode *left;
 * TreeNode *right;
 * TreeNode() : val(0), left(nullptr), right(nullptr) {}
 * TreeNode(int x) : val(x), left(nullptr), right(nullptr) {}
 * TreeNode(int x, TreeNode *left, TreeNode *right) : val(x), left(left), right(right) {}
 * };
 */
class Solution {
public:
    vector<int> inorderTraversal(TreeNode* root) {
        vector<int> result;
        traversal(root, result); // 召唤递归魔法
        return result;
    }

private:
    // 你的绝活：递归辅助函数
    void traversal(TreeNode* root, vector<int>& result) {
        // 保底防线（递归出口）：如果走到空气（nullptr）了，直接回头
        if (root == nullptr) {
            return;
        }

        // 1. 左：无脑甩锅给左半边
        traversal(root->left, result);
        
        // 2. 中：左边完事了，把自己存进去
        result.push_back(root->val);
        
        // 3. 右：最后甩锅给右半边
        traversal(root->right, result);
    }
};
3. 复杂度：魔法背后的代价
时间复杂度：O(N)。因为每个节点都会且仅会被访问一次，毫无悬念的线性时间。

空间复杂度：O(H)（H 是树的高度）。这里的空间不是指 result 数组，而是系统帮你维持的“函数调用栈”。如果是极其不平衡的树（退化成链表），空间就是 O(N)；如果是完美的平衡树，空间就是 O(logN)。



*/
//我的代码：
class Solution {
public:
    vector<int> result;


    void traversal(TreeNode* root)
    {
        if (root == nullptr) return;
        traversal(root->left);
        result.push_back(root->val);
        traversal(root->right);
        return;
    }

    vector<int> inorderTraversal(TreeNode* root) {
        result.clear();
        traversal(root);
        return result;
    }
};


/*
使用显性栈解决这个问题：

问题：但是你也知道，递归在树特别深的时候，会导致系统栈溢出（Stack Overflow）。你能在不使用系统隐式调用栈的情况下，也就是用『迭代（迭代循环 + 手动维护一个 Stack）』的方式，把中序遍历再写一遍吗？

解题思路：
在面试中，写出递归只能算及格（因为太简单了），但如果你能行云流水地写出显式栈（Stack）的迭代法，面试官会在心里直接给你打上“核心骨干”的思想标签。

让我们扔掉系统给的魔法拐杖，亲手用数据结构来接管一切！

1. 核心思维：一路向左，撞墙回头
中序遍历的口诀是：左 → 中 → 右。
这意味着，当我们站在根节点时，我们绝对不能立刻处理它！我们必须把它先“扣押”起来，然后发疯一样地往它的左下角钻，直到钻进死胡同为止。

破局战术：“模拟系统调用栈”三步曲
我们需要借用一个栈（Stack，后进先出），外加一个游标指针 curr。

疯狂向左冲刺（入栈）：
只要 curr 还有左孩子，我们就一路往左走。每走过一个节点，就把它压进栈里（相当于告诉系统：“我先去处理左边了，等会儿我还要回来处理你和你的右半边！”）。

撞到南墙，回头处理（出栈 + 记录）：
当 curr 走到空（nullptr）时，说明左边彻底没路了。这时候，我们从栈顶弹出一个节点。
此时弹出这个节点，意味着它的左子树已经全部处理完毕了！ 根据“左 → 中 → 右”的法则，现在该轮到它自己（中）出场了！把它的值存进结果数组里。

转向右路，重新冲刺：
处理完自己后，下一步该处理谁？当然是它的右孩子！我们让 curr = 弹出的节点->right，然后重复第 1 步，继续对右子树进行疯狂向左冲刺。

2. C++ 满分极客代码（无情迭代器）
仔细看这个 while 循环的条件：curr != nullptr || !st.empty()。
意思是：只要游标还在树上走，或者栈里还有被扣押的节点没处理完，探索就绝对不能停！

C++
/**
 * Definition for a binary tree node.
 * struct TreeNode {
 * int val;
 * TreeNode *left;
 * TreeNode *right;
 * TreeNode() : val(0), left(nullptr), right(nullptr) {}
 * TreeNode(int x) : val(x), left(nullptr), right(nullptr) {}
 * TreeNode(int x, TreeNode *left, TreeNode *right) : val(x), left(left), right(right) {}
 * };
 */
#include <vector>
#include <stack>

using namespace std;

class Solution {
public:
    vector<int> inorderTraversal(TreeNode* root) {
        vector<int> result;
        stack<TreeNode*> st;  // 我们的显式调用栈
        TreeNode* curr = root; // 探路游标

        // 只要游标没走到黑，或者栈里还有存货，就继续干活
        while (curr != nullptr || !st.empty()) {
            
            // 阶段 1：疯狂向左冲刺，沿途把节点全部扣押进栈
            if (curr != nullptr) {
                st.push(curr);     // 留作回头的路标
                curr = curr->left; // 头也不回地往左走
            } 
            // 阶段 2 & 3：撞到南墙了，开始回头处理，并转向右边
            else {
                // 此时 curr 是 nullptr，但栈里肯定有东西
                TreeNode* node = st.top(); // 拿出最近被扣押的节点
                st.pop();

                // 它的左边绝对已经走完了，现在轮到它自己出场（左 -> 中）
                result.push_back(node->val);

                // 它的活干完了，接下来该去搞定它的右子树了（中 -> 右）
                // 把探路游标扔到右孩子上，下一轮循环会继续对右子树执行“向左冲刺”
                curr = node->right; 
            }
        }

        return result;
    }
};
3. 复杂度分析发生了怎样的质变？
时间复杂度：依然是严格的 O(N)。虽然里面有 while 嵌套了 if-else，但从物理轨迹上看，每个节点仅仅是进栈一次、出栈一次。

空间复杂度：O(H)。其中 H 是树的高度。但是，这一次的内存是我们实打实在堆（Heap）上手动 new 出来（通过 std::stack 底层）的，彻底排除了导致程序崩溃的系统调用栈溢出（Stack Overflow）风险！ 工业界极其青睐这种安全可控的写法。

*/

//我的代码：
class Solution {
public:
   
    vector<int> inorderTraversal(TreeNode* root) {
        vector<int> result;
        stack<TreeNode*> st;
        TreeNode* curr = root;


        while (curr!=nullptr|| !st.empty())
        {
            if (curr != nullptr)
            {
                st.push(curr);
                curr=curr->left;   //易错点
            }
            else
            {
                TreeNode* node = st.top();
                st.pop();
                result.push_back(node->val);
                curr = node->right;//易错点
            }
        }
        return result;
    }
};
